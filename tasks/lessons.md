# Leçons Apprises

[2026-08-13] | La configuration Nginx en production servait le dashboard React depuis le dossier `/var/www/smartmaintenance/mct-maintenance-dashboard/build` au lieu de `/var/www/smartmaintenance/dashboard`. | Toujours vérifier le dossier `root` configuré dans le fichier de site Nginx actif (`/etc/nginx/sites-available/smartmaintenance`) lors de la synchronisation du bundle frontend.
[2026-08-13] | Une fonction d'autorisation (ex: `buildInterventionListWhere`) appelée dans un contrôleur Express mais non exportée par son module provoquait un crash 500 silencieux en production. | Toujours valider les exports des modules de politique d'accès et exécuter la suite de tests Jest avant tout redémarrage applicatif.
[2026-08-13] | Les événements d'authentification Socket.IO côté client (ex: `socket.on('authenticate', (userId) => ...)` les yeux fermés) permettent l'usurpation d'identité en s'abonnant aux rooms d'autres clients. | Exiger un middleware de poignée de main `io.use(socketAuth)` qui valide le jeton JWT au moment du handshake et dériver l'identité et les rooms uniquement à partir du jeton vérifié.
[2026-08-13] | Une directive Nginx générique `location /uploads/` sert tous les fichiers du disque en direct, contournant l'authentification et les règles de confidentialité Express pour les photos confidentielles et rapports. | Restreindre les alias statiques Nginx aux seuls répertoires explicitement publics (`avatars`, `products`, `equipments`, `qrcodes`) et laisser les médias sensibles (interventions, documents) passer par les routes Express authentifiées (`fileRoutes.js`).
[2026-08-13] | La carte de santé du système sur le Dashboard basculait en avertissement orange pour Redis car la route `/ready` n'incluait pas le champ `redis` dans sa réponse JSON. | Inclure systématiquement l'état de toutes les dépendances critiques (`database`, `redis`) avec un statut canonique (`connected`, `memory_fallback`, `disconnected`) dans la réponse JSON de l'endpoint `/ready`.

[2026-08-02] | Pour les écrans de démarrage/splash screen, séparer les animations d'entrée uniques (logo, titre, sous-titre, footer) des animations de chargement indéfinies (bulles/points de chargement). | Utiliser deux `AnimationController` distincts avec `TickerProviderStateMixin` : un `_entryController` appelé une seule fois avec `.forward()` pour fixer les éléments visuels à 100% après 1000ms, et un `_loadingDotsController` appelé en boucle avec `.repeat()` pour faire défiler uniquement les bulles de chargement.
[2026-08-02] | Lors de l'alignement côte à côte de deux boutons dans un `Row` avec des longueurs de texte différentes (ex: « Injoignable » 11 caractères vs « Démarrer » 8 caractères), une répartition par défaut `flex: 1` vs `flex: 2` provoque un retour à la ligne du bouton le plus long sur petit écran. | Utiliser un ratio adapté (ex: `flex: 4` et `flex: 5`), réduire la marge interne du bouton (`padding: EdgeInsets.symmetric(horizontal: 8)`), ajuster la taille de l'icône à 18px et forcer `maxLines: 1` avec `TextOverflow.ellipsis`.
[2026-08-02] | Les champs de texte libre « Nature de l'intervention » et « Observations » de l'écran de rapport de fin d'intervention (`create_report_screen.dart`) ont été supprimés sur demande explicite. Pour maintenir la rétrocompatibilité des payloads de l'API backend, fournir des valeurs par défaut non vides (ex: `'Entretien / Maintenance'`).
[2026-08-02] | Lorsque le statut d'une intervention est déjà à "En cours" (`in_progress`), le rapport doit impérativement s'ouvrir en mode rapport d'intervention final (`isInitialStep = false`). Ne jamais réouvrir le mode "Constat Initial (Démarrage)" avec le bouton "Valider & Démarrer l'intervention" sur une intervention déjà en cours d'exécution.
[2026-08-02] | Dans le workflow d'entretien/maintenance, lors du clic sur le bouton d'action (« Démarrer » ou « Rédiger le rapport »), la vérification de l'achèvement du constat initial (points 1 à 6) doit être systématique avant d'ouvrir la phase après intervention (points 7 à 9). | Toujours tester si `initial_completed == true` sur l'intervention. Si `false`, ouvrir `CreateReportScreen(isInitialStep: true)` pour forcer la saisie du constat avant intervention (points 1-6) avant le passage au statut "En cours". Une fois validé, l'ouverture suivante charge automatiquement l'Étape 2 pour les points 7 à 9.
[2026-08-02] | Pour les écrans de liste avec filtres et cartes de synthèse KPI (`invoices_screen.dart`), des cartes d'en-tête trop hautes réduisent fortement la visibilité de la liste d'éléments. | Réduire le padding et la taille des icônes/polices des cartes de synthèse KPI en en-tête (ex: padding 8px, icône 16px, valeur 17px) pour libérer plus de 100px d'espace vertical et afficher immédiatement plus de cartes de contenu sans défiler.
[2026-08-02] | Lors de la confirmation d'un paiement (`isPaid == true`), `payment_screen.dart` et `diagnostic_payment_screen.dart` utilisaient une tâche asynchrone non fiable (`Future.microtask`) dans la fermeture d'un `showDialog`, ce qui empêchait la fermeture de l'écran de paiement et la redirection vers le devis. | Toujours attester l'affichage du dialogue avec `await showDialog(...)` puis appeler directement `Navigator.of(context).pop(true)` pour garantir la fermeture systématique de l'écran de paiement et le retour au devis ou à la vue détaillée (`QuoteDetailScreen`).
[2026-08-02] | Une incohérence est survenue pour un acompte devis 50% de 5 FCFA : l'application mobile affichait 3 FCFA (`Math.ceil(5/2)`) tandis que l'API générait le lien FineoPay à 2 FCFA (`Math.floor(5/2)`). | Harmoniser la règle de calcul 50/50 sur l'ensemble de la stack : le premier acompte (étape 1) doit **toujours être arrondi au supérieur** (`Math.ceil(total / 2)`), et le solde (étape 2) correspond au **reste exact** (`total - premier_acompte`).
[2026-08-02] | Au clic sur le bouton « Terminer » d'une carte d'intervention en cours dans la liste (`interventions_screen.dart`), l'application appelait directement `completeIntervention` et fermait l'intervention sans ouvrir l'écran de rédaction de rapport (`DiagnosticReportScreen` ou `CreateReportScreen`). | Pour toute intervention en cours nécessitant un rapport (dépannage, installation, diagnostic, entretien), le clic sur le bouton « Terminer » depuis la liste doit ouvrir l'écran de rédaction de rapport approprié au lieu d'invoquer immédiatement `completeIntervention`.
[2026-08-02] | Dans le formulaire de rédaction de rapport (`create_report_screen.dart`), les en-têtes et étiquettes de sous-sections utilisaient du Vert pour les phases AVANT intervention et de l'Orange pour APRÈS intervention, créant une incohérence avec la convention visuelle globale de l'application. | Harmoniser l'ensemble des écrans de saisie et de consultation des rapports : **ORANGE pour les phases / données AVANT intervention** (constat initial) et **VERT pour les phases / données APRÈS intervention** (travaux terminés).
[2026-08-02] | Lors de l'ouverture du rapport pour une intervention de type `execution` (issue de la validation/paiement d'un devis), `_viewReport` ouvrait `ViewReportScreen` qui cherchait uniquement `report_data` et affichait un écran vide (« Aucun rapport disponible »). | Pour les interventions de type `execution` ou comportant un `diagnostic_report_id`/`diagnosticReports`, rediriger vers `ViewDiagnosticReportScreen` qui extrait et affiche le rapport de diagnostic initial ayant généré le devis et l'intervention de travaux.
[2026-08-02] | Un dépassement de disposition (`A RenderFlex overflowed by 32 pixels on the right`) est survenu sur l'écran `view_diagnostic_report_screen.dart` au niveau des titres de section comportant des textes longs (ex: `7/ Matériels nécessaires (Dépannage / Installation)`). | Toujours entourer le widget `Text` par un `Expanded` lorsqu'il est placé dans une `Row` aux côtés d'une icône pour permettre le retour à la ligne automatique et empêcher tout overflow visuel sur les petits écrans.
[2026-08-02] | La galerie photos du rapport (`_buildPhotosGallerySection`) n'affichait aucune image car `extractUrls` ne vérifiait que `data['image_url']` direct sur la Map globale du rapport/intervention, ignorant les listes `photos_before`, `photos_after`, `photos`, `images`, `report_images` et les fichiers locaux. | Parcourir récursivement toutes les clés d'images possibles (`photos_before`, `photos_after`, `photos`, `images`, `report_images`, `equipments`) et prendre en charge les chemins distants (`/uploads/...`) ainsi que les chemins de fichiers locaux (`File(path).existsSync()`).
[2026-08-02] | Les données techniques dans les rapports d'intervention s'affichaient verticalement sur une seule colonne et avec une association de couleurs prêtant à confusion (Vert pour AVANT intervention, Orange pour APRÈS). | Présenter systématiquement les constantes techniques sous forme de badges/chips horizontaux côte à côte (`Wrap` avec `_buildMeasureItem` compact), et appliquer la convention visuelle métier : **ORANGE pour AVANT Intervention** (état initial/constaté) et **VERT pour APRÈS Intervention** (état rétabli/final).
[2026-08-02] | Si la date/heure de démarrage (`started_at`) n'était pas enregistrée lors de l'ouverture du rapport, `_startTime` et `_endTime` prenaient tous deux `TimeOfDay.now()` à la même minute (ex: 16:58), produisant une durée par défaut de `0 min`. | Si `_startTime` est égal ou supérieur à `_endTime` lors du chargement, calculer automatiquement l'heure de début par défaut 30 minutes avant l'heure de fin actuelle pour garantir une durée initiale cohérente, tout en conservant les sélecteurs d'heure éditables par le technicien.
[2026-08-02] | Le technicien recevait une erreur `403 Accès non autorisé` lors de la consultation des devis car `quoteWorkflowController.js` et `resourceOwnershipPolicy.js` tentaient d'accéder au champ inexistant `assigned_to` au lieu de `technician_id` (ou `diagnosticReport.technician_id`), et l'écran `ViewDiagnosticReportScreen` n'affichait pas la section du devis généré. | Utiliser systématiquement `technician_id` pour la vérification des droits d'accès des techniciens sur les interventions/devis, inclure `quote` dans la réponse de `getInterventionById`, et afficher un composant `_buildQuoteSection` dédié dans `ViewDiagnosticReportScreen`.
[2026-08-02] | Le technicien recevait une erreur `403 Accès non autorisé` lors de la consultation des devis car `quoteWorkflowController.js` et `resourceOwnershipPolicy.js` tentaient d'accéder au champ inexistant `assigned_to` au lieu de `technician_id` (ou `diagnosticReport.technician_id`), et l'écran `ViewDiagnosticReportScreen` n'affichait pas la section du devis généré. | Utiliser systématiquement `technician_id` pour la vérification des droits d'accès des techniciens sur les interventions/devis, inclure `quote` dans la réponse de `getInterventionById`, et afficher un composant `_buildQuoteSection` dédié dans `ViewDiagnosticReportScreen`.
[2026-08-02] | La pop-up de confirmation de travaux (`_showPendingConfirmationDialog`) affichait un encadré rouge d'avertissement en dur (« Important : Le solde doit être réglé avant la dernière intervention de maintenance ») pour TOUTES les confirmations d'intervention, même si le paiement 100% avait été effectué ou s'il s'agissait d'un devis standard hors contrat de maintenance. | Ne jamais hardcoder un message d'avertissement de paiement fractionné dans une modale générique de fin de travaux ; vérifier si `payment_option == 'split'` et `second_payment_status != 'paid'`, masquer l'encadré rouge si aucun solde n'est dû, et adapter le texte selon qu'il s'agit d'un devis ou d'un contrat de maintenance.
[2026-08-02] | Lors de la fermeture des dialogs de succès de paiement (`payment_screen.dart`, `diagnostic_payment_screen.dart`), l'appel synchrone `Navigator.pop(dialogContext)` suivi immédiatement de `Navigator.pop(context)` dans le même tour de boucle échouait ou était ignoré car le `Navigator` était en pleine animation de transition de route, laissant la page de paiement affichée. | Capturer le `Navigator` parent avant la fermeture de la modale (`final localNav = Navigator.of(context);`), fermer la modale avec `dialogContext`, puis dépiler l'écran de paiement de façon asynchrone dans un `Future.microtask(() { if (localNav.canPop()) localNav.pop(true); });`.
[2026-08-02] | L'enregistrement transactionnel de paiement (`recordDiagnosticPayment`, `recordSubscriptionPayment`) échouait silencieusement sous PostgreSQL avec l'erreur `FOR UPDATE cannot be applied to the nullable side of an outer join` car `findByPk` combinait `lock: transaction.LOCK.UPDATE` avec des `include` (outer join). | Ne jamais utiliser `lock: transaction.LOCK.UPDATE` avec une option `include` sous PostgreSQL ; verrouiller le modèle principal sans jointure externe et charger séparément les relations si nécessaire.
[2026-08-02] | La vérification active des paiements FineoPay (`verifyDiagnosticPaymentStatus`, `verifyPaymentStatus`, `verifySubscriptionPaymentStatus`) échouait et bouclait indéfiniment en attente car l'API FineoPay `/transactions` ne renvoie pas de champ `syncRef`, rendant la recherche `transaction.syncRef === syncRef` toujours fausse. | Effectuer le rapprochement de paiement en vérifiant si `transaction.payLink.title` contient la référence/ID et si le montant correspond, avec `syncRef` comme fallback secondaire.
[2026-08-02] | La route `GET /api/customer/quotes` utilisait `[Op.in]` dans la clause `where` sans importer `const { Op } = require('sequelize');` en haut de `customerRoutes.js`, provoquant une erreur HTTP 500 (`Op is not defined`) au chargement de la liste des devis. | S'assurer que `const { Op } = require('sequelize');` est importé en haut de chaque fichier de routage Express dès qu'un opérateur de comparaison Sequelize est utilisé.

[2026-08-02] | Le générateur PDF des devis (`quoteController.js`) centrait le logo en en-tête (`(595.28 - 100) / 2`) alors que le générateur de factures l'alignait sur la marge gauche. | Harmoniser la position du logo d'en-tête sur l'ensemble des documents PDF générés par l'API (`x = 40`, marge gauche officielle) pour garantir une charte graphique uniforme.

[2026-08-02] | Le téléchargement de la facture PDF par l'application mobile interroge l'API distante Sandbox (`https://api.sandbox.mct.ci/api/payments/invoice/:id/download`). Une modification locale du logo `logo-maintenance.png` ou de `pdfService.js` ne s'applique sur les PDF téléchargés qu'une fois redéployée et redémarrée sur le serveur VPS. | Après tout changement d'asset ou de générateur PDF sur le backend, déployer immédiatement avec `./deploy/deploy.sh api` et vérifier la disponibilité avec `/live` avant de retester le téléchargement mobile.

[2026-08-02] | Remplacer uniquement l'image dans assets/images/ sur l'app mobile laissait le générateur PDF du backend API (`pdfService.js`) avec l’ancien logo `logo-maintenance.png`. | Lors de la mise à jour d’un visuel de marque ou de logo pour des documents générés, synchroniser simultanément les assets mobiles Flutter (`assets/images/`) et le dossier public backend (`mct-maintenance-api/public/logo-maintenance.png`).

[2026-08-02] | Une divergence d'architecture native (`incompatible platform: have iOS, need iOS-simulator`) sur `objective_c.framework` survient lorsque des caches FFI/Dart build subsistent après une mise à jour de dépendances. | En cas d'erreur de plateforme dylib/framework natif sur iOS Simulator, exécuter la séquence complète `flutter clean`, `flutter pub get`, `pod install` dans `ios/`, puis stopper et relancer la compilation de l'application.

[2026-08-02] | L'utilisation de `Navigator.pop(context)` après l'ouverture d'un dialog (`showDialog`) dans un onglet `_TabShell` à `Navigator` imbriqué supprimait la seule route locale du hub au lieu de fermer le dialog racine, vidant l'historique (`_history.isEmpty`) et provoquant un crash Flutter. | Cibler explicitement `Navigator.of(context, rootNavigator: true)` lors de la fermeture des modales/dialogs ouvertes sur le Navigator racine, et sécuriser les Navigators imbriqués avec un `PopScope` qui délègue au Navigator racine lorsque la pile locale ne contient qu'un seul élément.

[2026-08-02] | Un déploiement complet de l'API aurait activé en production l'exigence `REDIS_URL` alors que le sandbox ne la possédait pas, avec un risque d'échec au redémarrage pour une correction PDF indépendante. | Avant tout déploiement, vérifier les nouvelles préconditions d'environnement; si elles ne sont pas satisfaites, livrer uniquement l'artefact autonome nécessaire, conserver une sauvegarde, utiliser un rechargement progressif et confirmer `/live`, `/ready` et tous les workers.

[2026-08-02] | La génération des factures reposait sur Chromium via Puppeteer sur un VPS ne disposant pas de `libnspr4.so`, transformant une fonction métier simple en dépendance fragile à un navigateur et à de nombreuses bibliothèques système. | Pour les documents structurés simples, utiliser le moteur PDF natif déjà présent (`PDFKit`), tester la signature et la taille du fichier produit, et réserver un navigateur headless aux rendus HTML qui l'exigent réellement.

[2026-08-02] | L'écran des factures utilisait l'identifiant composite d'affichage `order-55` comme clé de commande et appelait une ancienne route inexistante, produisant un HTTP 500 « Route non trouvée ». | Conserver séparément l'identifiant métier d'affichage et la clé numérique `orderId`, typer les méthodes de dépôt avec un entier, utiliser la route canonique déclarée par le backend et masquer les actions qui exigent une commande lorsqu'aucun `orderId` n'est disponible.

[2026-08-02] | Vérifier uniquement l'extension et le MIME fournis par le client permettait d'envoyer un exécutable renommé en image ou un faux PDF. | Après l'écriture temporaire Multer, vérifier les magic bytes correspondant à l'extension, supprimer immédiatement l'ensemble des fichiers de la requête si l'un est invalide et retourner HTTP 415 avant tout traitement métier.

[2026-08-02] | Plusieurs endpoints authentifiés de devis, factures, paiements, contrats et rapports chargeaient une ressource par son ID sans vérifier son propriétaire; être connecté ne constitue pas une autorisation. | Centraliser les règles d'appartenance selon l'identité canonique de chaque modèle, filtrer aussi les listes, appliquer la politique avant tout effet ou téléchargement et retourner 404 hors périmètre pour ne pas révéler l'existence de la ressource.

[2026-08-02] | La blacklist JWT et les limites de requêtes utilisaient un cache mémoire propre à chaque processus, donc une révocation ou une limite n'était pas partagée entre workers PM2. | En production, exiger `REDIS_URL`, partager blacklist et compteurs de rate limiting dans Redis et refuser le démarrage si Redis est absent; réserver le fallback mémoire aux tests et au développement.

[2026-08-02] | Un script ponctuel `run-pending-migrations.js`, des scripts `Scripts-api/` et le runner principal formaient plusieurs chemins de migration concurrents, tandis que des SQL historiques mélangeaient différents dialectes. | Garder une seule commande de déploiement fondée sur `migration_history`, exiger un contrat JS `up/down`, fournir un mode status sans mutation et classer les anciens SQL/scripts comme archives non exécutables.

[2026-08-02] | Les photos d'intervention étaient protégées au niveau des endpoints métier mais restaient lisibles sans authentification via le dossier statique `/uploads`; fermer uniquement l'index et les dotfiles ne protège pas le contenu. | Servir les médias sensibles par une route authentifiée qui retrouve leur ressource propriétaire et réutilise sa politique d'accès, limiter le statique à une liste explicite de dossiers publics et transmettre le Bearer token depuis les widgets d'image mobiles.

[2026-08-02] | Le démarrage de l'API exécutait `sequelize.sync({ alter: true })`, ajoutait des colonnes et corrigeait des statuts de paiement, rendant chaque lancement non déterministe et impossible à auditer. | Le processus HTTP doit uniquement vérifier la connexion; toute mutation de schéma ou de données doit vivre dans une migration versionnée, testée et exécutée avant la bascule applicative après sauvegarde.

[2026-08-02] | Les callbacks devis/souscription avaient une transaction financière mais continuaient avec des notifications et automatisations directes, tandis que les vérifications actives rapprochaient parfois une transaction par montant/date. | Inscrire tous les effets externes dans l'outbox au sein de la transaction financière et exiger `syncRef`, statut et montant exacts pour une vérification active; ne jamais utiliser un rapprochement heuristique pour confirmer un paiement.

[2026-08-01] | Les contrôleurs d'API monolithiques mélangeant validation des requêtes, calculs tarifaires et logique de souscription rendaient les tests d'intégration fragiles et la traçabilité des requêtes mobiles difficile sur le backend. | Extraire la logique de calcul métier dans des services dédiés (`InterventionCreationService`), et auto-injecter dynamiquement un header `X-Correlation-ID` dans le client HTTP Flutter (`BaseApiService`) pour lier chaque requête aux logs serveur.

[2026-08-01] | Dans Flutter, l'utilisation directe de `Navigator.of(context)` ou `context.read()` après des opérations `await` déclenche des avertissements linter `use_build_context_synchronously` et peut provoquer des erreurs à l'exécution si le widget s'est démonte pendant l'attente I/O. | Capturer le `NavigatorState` (`final nav = Navigator.of(context);`) ou l'objet métier avant les appels asynchrones, et vérifier systématiquement `if (!mounted) return;` avant tout appel d'interface graphique dépendant du contexte.

[2026-08-01] | Le modèle de données `Payment` incluait des champs `refund_amount` et `refund_reason` mais aucun suivi formalisé par demande de remboursement ni protection d'idempotence, risquant des doubles traitements d'annulation. | Créer un modèle de demande dédié (`RefundRequest`), centraliser la validation et le traitement dans `refundManagementService.js`, exiger une clé d'idempotence (`idempotency_key`) et verrouiller les transactions pour garantir des remboursements 100% atomiques et idempotents.

[2026-08-01] | L'endpoint `/health` exposait des métriques système internes (`memoryUsage`, `nodeVersion`, `platform`), et les logs d'erreurs pouvaient inclure les corps de requêtes contenant des mots de passe ou tokens. | Séparer strictly `/live` (liveness épuré) et `/ready` (readiness vérifiant la DB), imposer la génération d'un `x-correlation-id` (`correlationMiddleware.js`), et appliquer un masquage récursif des secrets PII (`piiMasker.js`) sur les requêtes et métadonnées journalisées.

[2026-08-01] | Les contrôles de transitions d'état d'intervention étaient éparpillés dans chaque méthode de contrôleur sans matrice unique, risquant des transitions inattendues ou des contournements de préconditions. | Centraliser toutes les règles de transition et la validation des préconditions métier dans un service unique (`interventionStateMachineService.js`), et adosser des tests unitaires exhaustifs pour garantir le respect strict du workflow d'intervention.

[2026-08-01] | Le switch de synchronisation mobile SQLite sur `item['type']` exécutait un `default` passif sans lever d'exception, ce qui entraînait la suppression silencieuse et définitive des requêtes d'un type non géré ou invalide après leur passage en file d'attente. | Dans toute boucle de synchronisation file d'attente/outbox, lever systématiquement une exception explicite pour tout type d'événement inconnu afin de déclencher le mécanisme de retry/backoff et d'isoler l'élément dans la file d'attente des échecs (dead-letter).

[2026-08-01] | Les valeurs numériques de `User.id` et `CustomerProfile.id` peuvent entrer en collision : deviner le type d'un `Intervention.customer_id` selon l'existence de la valeur peut rattacher silencieusement une intervention au mauvais client. | Définir un invariant par clé étrangère, ici `Intervention.customer_id → customer_profiles.id`; dériver le profil depuis l'utilisateur authentifié, exiger explicitement un `CustomerProfile.id` pour les rôles internes, utiliser la contrainte existante comme source de vérité pendant le backfill et refuser toute base ambiguë sans contrainte.

[2026-08-01] | Une mise à jour générique d'intervention acceptait les champs d'identité `customer_id`/`customerId` du corps HTTP, permettant de changer le propriétaire sans passer par la résolution canonique. | Exclure systématiquement les clés d'identité des payloads de mise à jour génériques et réserver tout transfert de propriété à une opération dédiée, autorisée, auditée et validée contre la table référencée.

[2026-08-01] | Le fichier `.env.example` contenait des mots de passe SMTP et clés API réels en dur, et la route statique `/uploads` servait les fichiers sans interdire les dotfiles. | Toujours remplacer tous les credentials dans `.env.example` par des placeholders fictifs anonymes (`your_*`), exclure les fichiers de clés (`*.pem`, `*.key`, `*.p8`, `firebase-service-account.json`) dans `.gitignore`, et configurer `express.static` avec `dotfiles: 'ignore'` et `index: false`.

[2026-08-01] | Les endpoints de vérification active FineoPay (`/verify-payment`, `/verify-subscription-payment`, `/verify-diagnostic-payment`) permettaient à un rôle client connecté d'interroger les détails de statut d'une ressource sans validation de propriété. | Dans tous les endpoints de vérification active, valider systématiquement l'appartenance avec l'identité canonique propre à la ressource (`User.id` ou `CustomerProfile.id`, jamais un tableau ambigu pour les interventions) et renvoyer 404 si elle n'appartient pas au client.

[2026-07-31] | Passer plusieurs arguments à `console.error` (ex: `console.error(`msg ${id}:`, err.message)`) déclenche un avertissement de sécurité Semgrep `unsafe-formatstring` car Node.js utilise `util.format` sur le 1er argument. De plus, hardcoder `rejectUnauthorized: false` déclenche `bypass-tls-verification`. | Combiner les messages `console.error` dans une seule template string littérale `console.error(`msg ${id}: ${err.message}`)` et lier `rejectUnauthorized` à une variable d'environnement dynamique (`dbSslRejectUnauthorized`) pour garantir la propreté et la conformité sécurité Semgrep.

[2026-07-31] | Sur émulateur Android ou simulateur iOS, le plugin Flutter `connectivity_plus` peut renvoyer des résultats autres que wifi/mobile (`ConnectivityResult.other` ou `bluetooth`), ce qui faisait basculer `ConnectivityService.isConnected` à `false` (« Mode offline ») et forçait le repository à lire indéfiniment les anciennes interventions stockées dans la base SQLite locale sans jamais interroger l'API backend. | Dans `connectivity_service.dart`, évaluer la présence de connexion avec `results.isNotEmpty && !results.contains(ConnectivityResult.none)` pour éviter les faux positifs hors ligne sur émulateur/simulateur.

[2026-07-31] | Si l'identifiant client d'une intervention (`customer_id`) est enregistré tantôt sous forme de `CustomerProfile.id` et tantôt sous forme de `User.id`, les filtres doubles masquent l'incohérence et deviennent dangereux lorsque les espaces numériques se chevauchent. | Cette mesure transitoire est remplacée : utiliser exclusivement `CustomerProfile.id` pour `Intervention.customer_id`, avec une vraie clé étrangère, et ne jamais filtrer une intervention sur le tableau `[User.id, CustomerProfile.id]`.

[2026-07-31] | Lors de l'enregistrement du 1er acompte de 50% (`handleDiagnosticPayment` / `verifyDiagnosticPaymentStatus`), le serveur vérifiait `if (intervention.second_payment_status === 'pending')`, ce qui était vrai dès la création d'une intervention split et attribuait à tort le statut `Payé` au 2ème solde de 50%. | Vérifier en priorité `if (intervention.diagnostic_paid !== true)` pour marquer le 1er acompte de 50% comme payé. Ce n'est qu'une fois `diagnostic_paid === true` que le paiement suivant est attribué au solde fin d'intervention (`second_payment_status = 'paid'`).

[2026-07-31] | Lors de la réservation d'une planification d'intervention (« Planifier pour plus tard »), l'endpoint API `POST /api/quotes/:id/accept` créait la commande avec `status: 'scheduled'` et envoyait `notifyQuoteAccepted` aux admins/dashboard AVANT que le client ne réalise le paiement, transmettant immédiatement l'intervention au dashboard sans règlement. | Dans `customerRoutes.js` et `quoteController.js`, créer la commande initiale avec `status: 'pending'` et ne JAMAIS appeler `notifyQuoteAccepted` lors de `acceptQuote` ; déporter le déclenchement de `notifyQuoteAccepted` et le passage en `scheduled` dans le webhook/callback de paiement (`fineoPayController.js`) uniquement une fois le paiement confirmé.

[2026-07-31] | Lors de l'option « Planifier pour plus tard », l'intervention ne doit être confirmée et le message de confirmation de date affiché que SI le paiement (acompte 50% ou 100%) sur `PaymentScreen` a réussi (`paymentSuccess == true`). Si le paiement est annulé ou non finalisé, l'intervention ne doit pas être confirmée et un message d'avertissement invite à régler le paiement ci-dessous. | Capturer le résultat de `Navigator.push<bool>(PaymentScreen)` dans `_processAcceptQuote` et ne déclencher le pop-up de confirmation de la date planifiée que si `paymentSuccess == true`.

[2026-07-31] | Lors du paiement du solde (2ème paiement 50%), l'écran de paiement (`payment_screen.dart`) validait instantanément le paiement comme réussi dès l'ouverture de l'écran avant même que le client n'effectue le paiement sur FineoPay, car `_checkPaymentStatus` vérifiait `first_payment_status == 'paid'` ou `payment_status == 'partial'`, qui étaient déjà vrais depuis le premier acompte ! | Dans tous les écrans de paiement (`payment_screen.dart`, `diagnostic_payment_screen.dart`, `subscription_payment_screen.dart`), différencier impérativement la vérification de l'étape 2 (solde 50%) de celle de l'étape 1 : à l'étape 2 (`paymentStep == 2`), exiger strictly `second_payment_status == 'paid'` ou `payment_status == 'paid'`, et ne JAMAIS valider la réussite sur `first_payment_status` ou `partial` !

[2026-07-31] | L'acceptation d'un devis pour une intervention planifiée (« Planifier pour plus tard ») contournait l'étape de paiement et affichait un message disant « Le paiement sera effectué le jour de l'intervention », alors que le client devait payer son acompte (50% ou 100%) immédiatement avant d'initier l'intervention. | Dans `_processAcceptQuote` de `quote_detail_screen.dart`, rediriger systématiquement le client vers `PaymentScreen` (option 50% ou 100%) qu'il choisisse l'exécution immédiate ou la planification pour plus tard, et n'afficher la confirmation et n'émettre les notifications qu'une fois le paiement initié/validé.

[2026-07-31] | Dans la carte d'information d'un devis accepté, lorsque le second paiement (solde 50%) ou le paiement 100% était réglé, le composant affichait encore par erreur « Paiement du solde (50%) en attente » car la condition ne vérifiait pas l'état `paymentStatus == 'paid'` ou `secondPaymentStatus == 'paid'`. | Dans `quote_detail_screen.dart`, évaluer en priorité si le devis est intégralement payé (`paymentStatus == 'paid'` ou `secondPaymentStatus == 'paid'`) pour afficher « 🟢 Deuxième paiement (solde 50%) effectué » (ou « 🟢 Paiement intégral effectué »), et ne basculer sur l'attente du solde que lorsque seul le premier acompte a été réglé.

[2026-07-31] | Dans l'écran de paiement du diagnostic (`diagnostic_payment_screen.dart`), `_buildImportantNote()` retournait `SizedBox.shrink()` pour les paiements de diagnostic, laissant un double espacement vertical `SizedBox(height: 24)` vide entre les cartes ainsi qu'un espace blanc indésirable au bas de l'écran. | Ne restituer le composant et son espacement que lorsque `isMaintenanceDeposit` est vrai, et envelopper le conteneur principal dans un `SafeArea` avec dimensions explicites `width: double.infinity` et `height: double.infinity`.

[2026-07-31] | L'endpoint `GET /api/interventions` renvoyait toutes les interventions de la base au client connecté lorsque la requête ne passait pas de paramètre `customer_id`, et l'endpoint `/api/customer/interventions` utilisait `where: { customerId: userId }` avec le mauvais nom de colonne et le mauvais ID (`User.id` au lieu de `CustomerProfile.id`). | Dans tout contrôleur d'interventions (`getAllInterventions`), forcer automatiquement le filtre `where.customer_id = customerProfile.id` lorsque `req.user.role === 'customer'` afin d'assurer une isolation stricte et étanche des données par client.

[2026-07-31] | La carte « Options de paiement » affichée avant l'acceptation du devis était redondante et perturbait l'utilisateur avant le choix final. | Supprimer intégralement le conteneur bleu « Options de paiement » dans l'écran de détails du devis (`quote_detail_screen.dart`), laissant les boutons « Accepter le devis » et « Refuser le devis » directement accessibles.

[2026-07-30] | Les cartes de rapport de maintenance pouvaient être très longues et encombrantes sur les petits écrans mobiles. | Ajouter un système de carte réductible/déroulante (`isExpanded`) avec un bouton d'en-tête et une barre de basculement inférieure munie d'une flèche (`Icons.keyboard_arrow_down` / `Icons.keyboard_arrow_up`), permettant à l'utilisateur de déplier/replier les détails à volonté tout en gardant le premier rapport ouvert par défaut.

[2026-07-30] | Les rapports de maintenance affichés côté client n'affichaient pas les deux étapes (AVANT vs APRÈS intervention) car l'endpoint `GET /api/customer/maintenance-reports` ne renvoyait pas les photos et données techniques séparées par étape (`before_*` vs `after_*`), et l'interface client retombait sur le format legacy 1 seule étape. | Mettre à jour l'endpoint API client pour extraire et formater systématiquement les données 2-étapes (`photos_before`, `photos_after`, `before_pression`, `after_pression`, etc.), et structurer le widget client mobile `maintenance_reports_screen.dart` avec des blocs distincts 🟢 AVANT (Constat initial) et 🟠 APRÈS (Clôture).

[2026-07-30] | Lors de la soumission d'une demande avec code promo, la page de paiement ne s'ouvrait pas car le backend renvoyait déjà le `diagnostic_fee` ajusté (ex: 3.5 FCFA), et l'application mobile déduisait `_promoDiscount` une seconde fois en frontend (`3.5 - 5 = 0 FCFA`), ce qui annulait l'ouverture de l'écran de paiement. | Lorsque le backend calcule et retourne le `diagnostic_fee` ajusté après application du code promo (`diagnosticFeeFromServer`), utiliser directement `paymentAmount = diagnosticFeeFromServer` côté mobile sans ré-effectuer de soustraction `_promoDiscount` secondaire.

[2026-07-30] | L'application Flutter a renvoyé l'erreur `'dispose' is already declared in this scope` suite à l'ajout accidentel d'une deuxième méthode `dispose()` dans la classe d'état Dart lors d'une modification multi-lignes. | Avant d'ajouter une méthode de cycle de vie Widget (`dispose`, `initState`, `didUpdateWidget`), effectuer une recherche dans la classe d'état existante pour éviter les déclarations doublonnées et vérifier immédiatement la compilation avec `flutter analyze`.

[2026-07-30] | Les utilisateurs ne pouvaient pas appliquer de code promotionnel lors de la création d'une intervention de maintenance ou d'un diagnostic direct, car le champ de saisie de code promo n'était présent que dans l'écran de checkout boutique. | Intégrer un bloc de saisie de code promo (`_buildPromoCodeSection`) réutilisable directement dans les formulaires de demande de service avec validation via l'API `/api/promotions/validate` et retransmission du `promo_code` et du `discount_amount` au backend.

[2026-07-30] | Lors de la création d'intervention depuis la section "Nos Services", le type (Maintenance, Installation, Dépannage) était sélectionné mais restait modifiable dans le dropdown, ce qui permettait de changer par erreur le type d'intervention en cours de saisie. | Définir un getter `_isTypeLocked` basé sur la présence de `widget.preSelectedType`, et désactiver `onChanged: _isTypeLocked ? null : (v) => ...` dans le `DropdownButtonFormField` avec une icône de cadenas 🔒 et une note indicative explicite.

[2026-07-30] | Dans l'écran de détail d'intervention de l'application client, la carte technicien ne s'affichait pas lorsque l'intervention était au statut "assigned" si l'objet `technician` était null en cache ou si les clés nom/prénom variaient (`first_name` vs `firstName`). | Conditionner l'affichage de la carte technicien via une fonction `_shouldShowTechnicianCard()` qui s'active dès que le statut est `assigned` (ou au-delà), et sécuriser le widget d'affichage (`_buildTechnicianCard()`) avec un fallback propre ("Technicien MCT") et une lecture tolérante aux clés snake_case et camelCase.

[2026-07-30] | L'application Flutter a renvoyé l'erreur de compilation `The getter 'AppConfig' isn't defined` dans `intervention_detail_screen.dart` lors d'un remplacement de bloc d'importation car `import 'package:mct_maintenance_mobile/config/environment.dart';` avait été involontairement supprimé. | Lors de la modification des déclarations d'importation en haut d'un fichier Dart/JS, s me rassurer de préserver tous les imports utilitaires existants (ex: `environment.dart`) et vérifier immédiatement la compilation avec `flutter analyze`.

[2026-07-30] | L'option de tri `order: [['createdAt', 'DESC']]` échouait avec `column Intervention.createdAt does not exist` sur PostgreSQL car PostgreSQL traite la colonne `createdAt` comme un identifiant sensible à la casse. | Pour ordonner les tables Sequelize sans risque d'incompatibilité de nommage entre dialectes SQL, trier par `[['id', 'DESC']]` au niveau de la base de données puis ordonner la liste combinée en mémoire JavaScript (`allReports.sort((a, b) => new Date(b.date) - new Date(a.date))`).

[2026-07-30] | L'API backend retournait une erreur 502 Bad Gateway Nginx car une parenthèse fermante en double `});` dans `technicianRoutes.js` provoquait une erreur de syntaxe Node JS au chargement du serveur, entraînant un crash en boucle des processus PM2. | Toujours valider la syntaxe du code Node.js localement avec la commande `node -c <fichier>` avant de lancer un déploiement sur le serveur.

[2026-07-30] | L'association `Intervention.belongsTo(CustomerProfile, { foreignKey: 'customer_id', as: 'customer' })` dans Sequelize cherchait à joindre `customer_profiles.id = customer_id` au lieu de `customer_profiles.user_id = customer_id`, ce qui pouvait échouer si la clé `targetKey` n'était pas précisée. | Dans les associations Sequelize où la clé étrangère pointe vers un champ unique autre que la clé primaire (ex: `user_id`), toujours spécifier explicitement `targetKey: 'user_id'`.

[2026-07-30] | L'option de tri `order: [['created_at', 'DESC']]` sur le modèle `Intervention` dans `technicianRoutes.js` générait l'erreur SQL `Unknown column 'created_at' in 'order clause'` car le nom de propriété Sequelize est `createdAt` (mappé via la configuration du modèle `createdAt: 'created_at'`). | Dans les requêtes Sequelize `.findAll({ order: [...] })`, toujours utiliser le nom de la propriété du modèle JS (ex: `createdAt`) et non le nom brut de la colonne SQL (`created_at`).

[2026-07-30] | La route `GET /api/technician/reports` retournait une erreur HTTP 500 ("Erreur de base de données") car la clause `$intervention.technician_id$` utilisée avec `required: false` provoquait une erreur de génération SQL Sequelize sur PostgreSQL/MySQL. | Éviter la syntaxe `$association.field$` dans les clauses `where` Sequelize lorsque l'association est en jointure externe (`required: false`). Privilégier une requête en 2 étapes simples (1. Récupérer les IDs des interventions assignées au technicien, 2. Filtrer par `[Op.or]: [{ technician_id }, { intervention_id: { [Op.in]: ids } }]`) pour garantir un SQL 100% valide.

[2026-07-30] | L'onglet "Mes Rapports" de l'application mobile technicien affichait "Aucun rapport" car la route API `GET /api/technician/reports` (dans `technicianRoutes.js`) ne filtrait `DiagnosticReport` que sur `technician_id === req.user.id`, ignorant les rapports liés aux interventions assignées au technicien, et `equipments` n'était pas désérialisé correctement. | Dans les requêtes d'agrégation de rapports pour les techniciens, toujours croiser les critères d'association (par `technician_id` direct et via l'intervention parente `$intervention.technician_id$`), et vérifier la désérialisation JSON sécurisée pour tous les champs complexes (`equipments`, `parts_needed`, `photos`).

[2026-07-30] | Sur l'application mobile technicien, la vue de consultation de rapport de diagnostic affichait "Aucun rapport trouvé" car l'endpoint API `getTechnicianInterventions` n'incluait pas la relation `DiagnosticReport` (`as: 'diagnosticReports'`), et l'écran `ViewDiagnosticReportScreen` ne rechargeait pas les données fraîches depuis l'API. | Dans les contrôleurs API retournant des listes d'interventions (`getTechnicianInterventions`), toujours inclure la relation `DiagnosticReport` (alias `diagnosticReports`) et associer `diagnostic_report = diagnosticReports[0]`. Côté mobile, faire de la vue du rapport un `StatefulWidget` qui recharge automatiquement les données depuis l'API par ID (`getInterventionById`) si le rapport est absent de l'état local.

[2026-07-30] | Dans l'écran `intervention_detail_screen.dart`, pour les interventions avec le statut `execution_confirmed` (devis payé), le bouton d'action affichait directement "Terminer l'intervention" en sautant l'étape "Démarrer l'intervention". Le technicien ne pouvait donc pas passer l'intervention à l'état `in_progress`. | Dans le workflow d'exécution d'une intervention, lorsque le statut est `execution_confirmed`, le bouton d'action doit déclencher l'action `start` ("Démarrer l'intervention") pour faire passer l'état à `in_progress`. Ce n'est qu'une fois à l'état `in_progress` que le bouton doit afficher "Terminer l'intervention".

[2026-07-30] | L'ouverture d'un rapport de diagnostic sur le Dashboard React provoquait un plantage `TypeError: (intermediate value).map is not a function at DiagnosticReportsPage.tsx:446` car la propriété `selectedReport.equipments` (ou `parts_needed`) reçue de l'API était une chaîne de caractères JSON (ex: `'[{"type":"Mural"}]'`). Le test `equipments && equipments.length > 0` était vrai pour les chaînes de caractères non vides, mais l'appel `.map()` sur une String échouait. | Dans tout composant React/JS recevant des propriétés potentiellement sérialisées de l'API (ex: `equipments`, `parts_needed`), toujours exécuter un parsing sécurisé (`typeof data === 'string' ? JSON.parse(data) : data`) et vérifier `Array.isArray(data)` avant tout appel à `.map()`, `.forEach()` ou `.filter()`.

[2026-07-30] | L'application Flutter renvoyait une erreur de compilation `Error when reading 'lib/utils/snack_bar_helper.dart'` car le fichier réel s'appelle `snackbar_helper.dart` (sans tiret bas entre `snack` et `bar`). | Toujours vérifier le nom exact des fichiers dans le système de fichiers (`lib/utils/snackbar_helper.dart`) avant d'écrire la directive `import`.

[2026-07-30] | L'appel API GET `/api/interventions/:id` renvoyait une erreur HTTP 500 car le modèle `DiagnosticReport.js` avait été enrichi avec les colonnes `equipments` et `materials_needed`, mais ces nouvelles colonnes n'existaient pas encore dans la table `diagnostic_reports` sur la base de données du serveur. | Lors de l'ajout de nouveaux champs sur un modèle Sequelize existant, toujours ajouter la vérification et le `queryInterface.addColumn()` correspondant dans le bloc d'auto-migration au démarrage de `app.js` pour que le schéma de la base de données soit synchronisé avant l'exécution des requêtes SQL.

[2026-07-29] | Le constat initial du rapport de maintenance pouvait disparaître à l’ouverture de la phase finale parce que l’écran dépendait d’une copie potentiellement obsolète de l’intervention. En mode hors ligne, `startIntervention` ne mettait en queue que le statut et abandonnait entièrement `report_data`; les photos restaient en plus des chemins temporaires. Enfin, la soumission finale pouvait remplacer le tableau `equipments` et rendre la perte définitive. | Un workflow de formulaire en plusieurs phases doit conserver un brouillon unique faisant autorité : le recharger par ID avant chaque reprise, mettre le payload complet et les médias durables dans la queue hors ligne, utiliser la même API authentifiée pour la synchronisation, protéger côté serveur les champs des phases déjà validées et ne supprimer un élément de queue qu’après un succès explicitement confirmé.

[2026-07-29] | La vérification du 2ème paiement (50% solde) d'une intervention échouait et restait bloquée à l'état non payé car `initializeDiagnosticPayment` générait un lien de paiement avec le montant `diagnostic_fee` (1er acompte de 50%) au lieu du `second_payment_amount` (2ème solde de 50%), tandis que `verifyDiagnosticPayment` filtrait les transactions sur le montant `second_payment_amount`. La différence de montant entraînait un échec d'association de la transaction FineoPay. | Toujours utiliser la même propriété de montant (`second_payment_amount` si `second_payment_status === 'pending'`, sinon `diagnostic_fee`) aussi bien lors de la génération du lien de paiement (`initializeDiagnosticPayment`) que lors de la recherche et vérification du statut (`verifyDiagnosticPayment`). S'assurer également d'autoriser la correspondance sur les deux montants potentiels dans `verifyDiagnosticPayment` pour récupérer les paiements déjà effectués.

[2026-07-29] | L'upload d'avatar Flutter échouait avec une erreur HTTP 500 (`Seules les images sont autorisées...`) car `http.MultipartFile.fromPath('avatar', imagePath)` était appelé sans `contentType`, ce qui transmettait `application/octet-stream` au serveur et faisait échouer le filtre Multer. | Dans tout appel Flutter `http.MultipartFile.fromPath`, toujours déduire et passer explicitement `contentType: MediaType.parse(mimeType)` selon l'extension du fichier. Côté backend, autoriser `application/octet-stream` (et les mimes équivalents `image/jpg`, `image/pjpeg`) dans le `fileFilter` Multer si l'extension du fichier est une extension d'image valide (`.jpg`, `.png`, `.webp`, etc.).

[2026-07-29] | `submitReport` dans `interventionController.js` rejetait la soumission du rapport de maintenance avec HTTP 400 (`L'intervention doit être terminée pour soumettre un rapport`) car la validation exigeait `status === 'completed'`. Or le flux correct est : technicien rédige le rapport **pendant** `in_progress`, et la soumission du rapport **déclenche** le passage en `completed`. | Dans le flux de rapport de maintenance, la validation de statut doit accepter `['in_progress', 'completed']`. Lors de la soumission, si l'intervention est `in_progress`, la passer automatiquement en `completed` avec `completed_at = new Date()` dans le même `update()`.



[2026-07-29] | En voulant supprimer le "2ème rapport post-exécution" (rapport redondant après un devis payé), la logique du `switch (status)` dans `intervention_detail_screen.dart` a été trop élargie et a supprimé le bouton "Rédiger le rapport" pour **tous** les types d'intervention `in_progress`, y compris maintenance, installation, dépannage et réparation. | Lors d'une demande de suppression d'une fonctionnalité partielle, toujours identifier précisément le sous-cas ciblé (ex: `intervention_type == 'execution' && quote_id != null`) et ne modifier que ce cas spécifique. Ne jamais supprimer un bloc de code générique (ex: tout le `else`) si seul un sous-ensemble de ses conditions est concerné. Documenter dans un commentaire de code le cas traité par chaque branche du `switch`.



[2026-07-12] | `chatService.js` faisait confiance au `userId` envoyé par le client dans l'événement `chat:authenticate` sans vérifier le JWT — n'importe qui pouvait se faire passer pour un autre utilisateur ou s'auto-attribuer le rôle `admin`. | Pour toute authentification Socket.IO, ne jamais accepter `userId` ou `role` en clair depuis le client. Exiger un JWT dans le payload, le vérifier avec `jwt.verify(token, process.env.JWT_SECRET)` côté serveur, et extraire l'identité depuis le token décodé. Stocker `userId` et `userRole` sur l'objet socket lors de la connexion.

[2026-07-12] | `ComplaintCreate.tsx` utilisait des appels `fetch('http://localhost:3000/api/...')` hardcodés avec récupération manuelle du token depuis `localStorage` — cassé en production et exposait des fragments de JWT dans les logs console. | Ne jamais faire d'appels API directs avec `fetch()` dans les pages React du dashboard. Toujours importer l'instance Axios centralisée (`import api from '../../services/api'`) qui injecte automatiquement le token Bearer, gère les erreurs 401/403 et pointe vers le bon host selon l'environnement.

[2026-07-12] | Les intercepteurs Axios de `api.ts` loggaient `response.headers`, `response.data`, `error.config.headers` (contenant le JWT) et le corps complet des requêtes/réponses en production. | Les logs d'un service API ne doivent jamais exposer des données sensibles. Conditionner tous les `console.log/error` à `process.env.NODE_ENV === 'development'` et limiter le contenu aux informations techniques : méthode HTTP, URL relative et code de statut.

[2026-07-12] | Le polling de `ContractPaymentScreen` tournait indéfiniment avec `Timer.periodic` sans aucun compteur — si FineoPay ne confirmait jamais le paiement (réseau, timeout), le timer restait actif sans jamais informer l'utilisateur. | Tout `Timer.periodic` de vérification de paiement doit avoir un compteur maximum (ex: 60 × 5s = 5 min). À l'expiration, annuler le timer, afficher un dialog explicite avec un bouton "Vérifier maintenant" pour relancer, et un bouton "Fermer" pour sortir proprement.

[2026-07-12] | `SubscriptionPaymentScreen` cherchait `response['data']?['checkoutUrl']` alors que `initializeSubscriptionPayment` retournait déjà `decoded['data']` (la sous-map). Le même endpoint backend utilise la clé `paymentUrl` (pas `checkoutUrl`). Résultat : l'URL était toujours null et le paiement échouait avec « Aucun lien de paiement reçu du serveur ». | Quand un repository extrait `decoded['data']`, l'écran doit lire les clés directement depuis `response` (ex: `response['paymentUrl']`). Ne jamais relier `response['data']` une deuxième fois. Toujours vérifier la clé exacte dans le contrôleur backend avant de coder l'extraction côté Flutter. Utiliser un fallback (`paymentUrl ?? checkoutUrl`) pour absorber les variations de nommage FineoPay.

[2026-07-10] | Le flux de paiement FineoPay affichait l'erreur JSON "Transaction non trouvée" après le paiement car le paramètre `autoRedirect: true` provoquait un traitement asynchrone côté fournisseur qui crashait leur serveur avant la redirection | Même lorsqu'une documentation d'API tierce propose une redirection automatique vers un Deep Link (ex: `autoRedirect: true`), tester la robustesse de leur serveur. S'il échoue, préférer une redirection manuelle (`autoRedirect: false`) avec un bouton de retour utilisateur qui laisse le temps à la transaction tierce de se finaliser en base.

[2026-07-10] | L'application mobile utilisait `notificationData['relatedId']` pour ouvrir les détails d'une intervention depuis une notification "Technicien sur place (client absent)". Mais le backend transmettait `relatedId` à la racine de la notification, qui était ignoré par la fonction `notificationService.create`. Le clic sur la notification échouait silencieusement et redirigeait vers le profil par fallback. | Toujours vérifier la signature et le traitement exacts des paramètres d'une fonction de service partagée. Si le service attend les métadonnées spécifiques dans une sous-clé `data`, il faut explicitement englober ses paramètres (`data: { relatedId: ... }`) lors de l'appel pour qu'ils soient persistés et envoyés correctement au frontend via FCM.
[2026-07-08] | Lors de l'appui sur un bouton "Déconnexion" situé dans un BottomSheet ou un Dialog, la méthode `navigator.pop()` était appelée **avant** `context.read<AuthRepository>()` pour fermer le menu. Cela rendait le contexte invalide (unmounted), ce qui levait une exception silencieuse lors de l'appel à `context.read` et provoquait l'annulation totale de la fonction `logout()`. L'utilisateur était redirigé vers l'écran de Login sans que les tokens ne soient supprimés ! | Toujours extraire les dépendances liées au contexte (ex: `final repo = context.read<Repo>();`) **avant** toute opération de navigation asynchrone ou de fermeture (`pop()`) qui risquerait de démonter le widget.
[2026-07-08] | Lors de la déconnexion, si la suppression du token via `FlutterSecureStorage` échouait silencieusement ou plantait (ex: bug lié au Keychain sur simulateur iOS/Mac qui survit aux hot restarts), le reste du processus de nettoyage (`SharedPreferences`, FCM) était interrompu, et l'utilisateur restait connecté au prochain redémarrage | Toujours isoler chaque étape critique du nettoyage (`logout` ou `clearAuthData`) dans son propre bloc `try...catch`. Surtout, implémenter un flag explicite (`has_logged_out`) dans `SharedPreferences` lors de la déconnexion, et vérifier ce flag en premier lieu dans `isLoggedIn()` pour ignorer toute valeur fantôme coincée dans la keychain de `FlutterSecureStorage`. Retirer le flag uniquement à la prochaine connexion réussie.

[2026-07-08] | Les formulaires d'inscription et de connexion supposaient un succès de l'opération même lorsque le backend renvoyait une erreur (ex: 409 Conflit ou 401 Unauthorized), ce qui provoquait des comportements inattendus ou des plantages avec des messages non explicites | Ne jamais présumer du succès d'une requête API simplement parce qu'aucune exception réseau (SocketException) n'est levée. Il faut toujours vérifier explicitement le statut de la réponse (ex: `response['success'] == true`) avant de parser les données, et extraire le champ `message` de l'API pour afficher la vraie erreur à l'utilisateur.

[2026-07-08] | La table SQLite locale n'avait pas la colonne `availability_info` dans `installation_services`, contrairement au modèle Sequelize, provoquant un plantage 500 silencieux bloquant l'affichage des requêtes | Toujours vérifier que la structure des tables de la base de données de développement correspond à la structure définie dans Sequelize, car les options comme `{ alter: true }` ne mettent pas toujours à jour proprement sur SQLite. Exécuter un `ALTER TABLE` si besoin.

[2026-07-08] | Lors de la création d'une nouvelle demande de maintenance, les utilisateurs devaient initialement payer l'acompte (50%) et le solde (50%) plus tard, mais préféraient payer la totalité en une seule fois | Toujours offrir plus de flexibilité dans les processus de paiement complexes. Si un système d'acompte 50/50 est implémenté, prévoir une option UX `Paiement intégral (100%)` pour les utilisateurs désirant s'acquitter de la somme dès la commande.

[2026-07-09] | Déploiement du Dashboard (React) : Les fichiers sources étaient sur le serveur mais non compilés, car `npm run build` échouait sans `package.json` | Un serveur web (comme Nginx) sert des fichiers statiques compilés, pas des sources React. Soit on build sur le serveur (nécessite `package.json`), soit on build en local et on utilise `scp -r build/* ...` pour transférer uniquement les fichiers statiques de production.
[2026-07-09] | Fausses erreurs CORS sur le Dashboard alors que la config CORS de l'API était correcte | Lorsqu'une API Node.js crashe complètement (ex: erreur de syntaxe), Nginx renvoie une erreur 502 Bad Gateway. Cette réponse 502 ne contient pas d'en-têtes CORS. Le navigateur intercepte la réponse et signale une erreur CORS ("No Access-Control-Allow-Origin"), masquant le crash serveur. Toujours lancer `node -c` pour vérifier la syntaxe avant d'investiguer une erreur CORS soudaine.
[2026-07-09] | Erreurs 404 sur des anciennes notifications après avoir corrigé les URLs dans l'API | Les corrections de routes générées par l'API (ex: passer de `/contracts` à `/contrats`) ne s'appliquent qu'aux *nouvelles* notifications. Les anciennes notifications stockées en base de données contiennent toujours l'URL obsolète. Il faut toujours ajouter une couche d'interception (fallback) côté frontend pour réécrire à la volée les anciennes URLs lors du clic.
[2026-07-09] | Validation stricte dans les formulaires Ant Design sur le Dashboard | Dans un `<Form.Item>` avec Ant Design (React), la règle `rules={[{ required: true }]}` bloque la soumission du formulaire et affiche l'astérisque rouge. Pour rendre un champ optionnel, il suffit de supprimer cette règle du `<Form.Item>`.

[2026-07-08] | Lors de la demande de maintenance (abonnements d'entretien), les paiements s'effectuaient à 100% au lieu d'être fractionnés (50/50) comme pour les devis, car l'application mobile redirigeait vers SubscriptionPaymentScreen au lieu de ContractPaymentScreen, et le backend n'associait pas les statuts de paiement split à l'activation | Configurer les abonnements à visites planifiées (scheduled) pour utiliser le flux de paiement fractionné (acompte 50% à la validation, solde 50% à la dernière visite). Dans l'app Flutter, utiliser le ContractPaymentScreen adapté et mettre à jour le filtrage des souscriptions actives pour accepter le statut de paiement 'partial' afin que le client puisse utiliser son abonnement dès le premier acompte payé.

[2026-07-08] | Le second paiement d'un devis en 50/50 pouvait rester bloqué parce que l'ordre gardait un `paymentStatus` déjà à `paid` et un `paymentStep` obsolète, alors que le devis était encore en état `partial` | Pour les paiements split, ne jamais se fier uniquement au statut `paid` de la commande. Déduire l'étape réelle depuis les statuts du devis (`first_payment_status` / `second_payment_status`), persister cet état sur l'ordre et continuer à attendre le second versement tant que le devis reste partiellement payé.

[2026-07-08] | Lors de la récupération d'entités contenant des données de secours en colonne JSON (ex: devis avec line_items au lieu d'une relation d'items Sequelize), certaines APIs ou le générateur PDF n'affichaient pas les articles | Toujours isoler l'extraction des données dans un helper unifié (ex. getQuoteItemsHelper) partagé par toutes les routes API et le générateur de PDF pour s'assurer que le contenu est identique partout.

[2026-07-08] | L'application mobile levait type 'Null' is not a subtype of type 'String' in type cast car certaines méthodes d'initialisation de paiement de PaymentRepositoryImpl renvoyaient le JSON racine au lieu du sous-objet 'data' attendu par les écrans de paiement | Assurer une cohérence stricte des types de retour dans les dépôts (repositories). Si un service/repository extrait la clé 'data' pour une méthode, s'assurer que toutes ses variantes de méthodes fassent de même pour éviter des valeurs nulles inattendues.

[2026-07-08] | Le bouton de paiement d'un devis n'apparaissait pas si l'utilisateur y retournait après avoir quitté le tunnel de paiement, car la condition d'affichage filtrait strictement sur paymentStatus == 'deferred' au lieu de proposer le paiement pour tout état non réglé | Ne pas baser la visibilité d'un bouton d'action critique sur un seul statut d'attente (comme deferred). Préférer un filtre large excluant uniquement le statut final de complétion (ex. paymentStatus != 'paid').

[2026-07-08] | Divergence potentielle entre le montant textuel affiché sur un bouton de paiement et le montant réellement traité lors de l'action de clic | Centraliser et harmoniser la logique de calcul de montant (et ses fallbacks de secours) afin que le libellé du widget bouton et la fonction d'action (ex: _payNow) s'appuient exactement sur les mêmes variables et règles.

[2026-06-15] | La récupération des souscriptions client renvoyait une erreur 500 car les colonnes de split payment (`first_payment_amount`, `first_payment_status`, `second_payment_amount`, `second_payment_status`) étaient absentes de la table `subscriptions` en SQLite local | Exécuter des requêtes `ALTER TABLE subscriptions ADD COLUMN ...` pour ajouter manuellement les colonnes manquantes liées au paiement fractionné et s'aligner sur la définition du modèle Sequelize.

[2026-06-15] | La modification d'un utilisateur renvoyait une erreur 500 sur SQLite local car la colonne `address` était absente de la table `customer_profiles` (Sequelize `{ alter: true }` ne gère pas toujours de manière fiable l'ajout de nouvelles colonnes sur SQLite) | En cas d'erreur 500 lors de requêtes Sequelize en base SQLite locale, inspecter le schéma de la table via `PRAGMA table_info(table)` et exécuter manuellement une commande `ALTER TABLE table ADD COLUMN ...` pour synchroniser le schéma physique avec le modèle Sequelize.

[2026-06-15] | Le formulaire de modification d'utilisateur (`UserForm.tsx` du dashboard) plantait avec une `TypeError` lors de l'appel de `email.trim()` car certains comptes (ex: les clients locaux sans adresse email renseignée en base) avaient la valeur `null` | Toujours initialiser les valeurs de formulaires provenant de l'API avec des valeurs de repli (ex. `|| ''`) et valider l'existence de la variable avant d'appeler des méthodes de chaîne de caractères comme `.trim()`.

[2026-06-15] | L'overlay d'erreur Webpack (Create React App) bloque l'écran de développement en local à cause d'une erreur d'extension Chrome injectée (ex: `Cannot read properties of undefined (reading 'addListener')` dans un Worker Blob de Loom ou Adobe) | Ajouter une règle CSS globale (`iframe#webpack-dev-server-client-overlay { display: none !important; }`) pour désactiver l'overlay client Webpack pour ces erreurs de scripts externes sans avoir à éjecter la configuration du projet.

[2026-06-15] | L'application plantait au démarrage local à cause de l'option SSL requise par PostgreSQL en production car `NODE_ENV` était défini sur `production` localement | S'assurer que `NODE_ENV` est défini sur `development` en local pour basculer sur la base SQLite locale ou configurer le serveur local sans contraintes SSL de production.

[2026-06-15] | L'application mobile appelait une route avec le mauvais préfixe (`/api/payments/fineopay/...` au lieu de `/api/fineopay/...`) entraînant un 404 car celle-ci était déclarée différemment dans le backend | Toujours s'assurer de la cohérence des préfixes de route entre le client mobile (ex. `BaseApiService`) et la déclaration des routeurs Express en vérifiant les fichiers de routing du backend.

[2026-06-15] | Les écrans de paiement utilisaient une vérification générique par référence textuelle locale (ex. `DIAG-xxx`, `SUB-xxx`) auprès de la passerelle de paiement tierce FineoPay, générant une erreur 500 car le tiers attendait des identifiants `TRX...` | Ne jamais envoyer de références textuelles locales à des endpoints de vérification directe de passerelles tierces. Utiliser des endpoints backend dédiés (comme `/verify-diagnostic-payment/:interventionId`) qui résolvent et font correspondre dynamiquement les transactions locales avec les transactions tierces.

[2026-05-08] | Singleton service (NotificationNavigationService, FCMService) crée son propre BaseApiService() → jamais de token → 401 silencieux sur toutes ses API calls | Tout singleton qui fait des appels API doit exposer un `setToken()` (ou `setAuthToken()`) et l'AuthRepository doit l'appeler partout où il met à jour le token : `isLoggedIn()`, `loadSavedToken()`, `_saveToken()`, `_clearAuthData()`

[2026-05-08] | Repository retournait le wrapper JSON entier `{success, data: {...}}` au lieu de `data` → le champ cherché était toujours null | Dans les repositories Flutter, toujours extraire `decoded['data']` après vérification de `decoded['success'] == true`. Ne jamais retourner le wrapper brut si l'appelant s'attend à des champs de données directement

[2026-05-08] | `POST /api/customer/update-fcm-token` et `POST /api/notifications/:id/mark-as-read` n'existent pas → 404/500 silencieux | Avant d'écrire un appel API Flutter, vérifier l'existence exacte de la route dans les fichiers de routes backend (méthode HTTP + chemin). Ne pas deviner les noms d'endpoints

[2026-04-07] | Notifications dupliquées envoyées car pas de vérification si déjà envoyée aujourd'hui | Toujours implémenter une logique de déduplication pour les jobs cron qui envoient des notifications

[2026-04-07] | Push notifications ne fonctionnent pas car le serveur de prod utilise un ancien fichier Firebase (projet différent) | Après changement de projet Firebase: 1) Mettre à jour firebase-service-account.json sur TOUS les serveurs 2) Redémarrer les serveurs 3) Les utilisateurs doivent se reconnecter pour régénérer leur token FCM

[2026-04-08] | firebase-admin SDK v13+ ne fonctionne pas pour envoyer des FCM (erreur auth interne) | Utiliser google-auth-library + HTTP v1 API directement via https natif au lieu de firebase-admin pour les notifications FCM

[2026-04-08] | Clé APNs uploadée uniquement en slot "développement" dans Firebase Console → iOS notifications échouent avec THIRD_PARTY_AUTH_ERROR | Firebase a deux slots séparés pour les clés APNs (dev et production). Toujours uploader la clé .p8 dans LES DEUX slots, même si Apple ne distingue plus dev/prod

[2026-04-08] | Token FCM envoyé à un appareil iOS avec THIRD_PARTY_AUTH_ERROR mais Android fonctionne | Quand Android marche mais iOS non → c'est un problème de clé APNs dans Firebase, pas un problème OAuth

[2026-04-08] | PM2 processes corrompus ("Process 0 not found") → 502 Bad Gateway | Quand pm2 restart échoue, utiliser: pm2 kill && pm2 start ecosystem.config.js

[2026-04-20] | 8 notifications identiques envoyées simultanément pour un même rappel de paiement | En mode PM2 cluster, les cron jobs s'exécutent sur CHAQUE worker. Toujours limiter les cron jobs au worker 0 avec: if (!process.env.NODE_APP_INSTANCE || process.env.NODE_APP_INSTANCE === '0')

[2026-04-20] | Notifications de paiement échouent silencieusement car les types (payment_failed, payment_confirmed, etc.) ne sont pas dans l'ENUM PostgreSQL du modèle Notification | Toujours vérifier que les types utilisés dans notificationService.create() existent dans l'ENUM du modèle Notification.js ET dans la base PostgreSQL (ALTER TYPE ADD VALUE)

[2026-04-20] | Dashboard ne reçoit pas les notifications Socket.IO en temps réel — l'utilisateur doit rafraîchir la page | En PM2 cluster mode, chaque worker a sa propre instance Socket.IO avec ses propres rooms en mémoire. Un événement émis par le worker C n'atteint jamais les sockets connectés au worker A. Solution : installer @socket.io/redis-adapter pour propager les événements entre tous les workers via Redis pub/sub

[2026-04-20] | Badge chat affiche "0000" au lieu de 0 | PostgreSQL retourne `unread_count` comme string depuis un CASE/COUNT. La concaténation "0" + "0" donne "00". Toujours utiliser `parseInt(value, 10) || 0` sur les compteurs PostgreSQL

[2026-04-20] | Variable `message` du callback Socket.IO masque `message` d'Ant Design | Nommer les paramètres de callback Socket différemment des imports globaux. Ex: `msg` au lieu de `message`

[2026-04-20] | Déploiement dashboard ne prend pas effet | Nginx sert depuis `/build/` sous-dossier. La cible scp doit être `.../mct-maintenance-dashboard/build/` et non `.../mct-maintenance-dashboard/`

[2026-04-20] | Graphique "Répartition par Type" affiche tout en "Non spécifié" | Le backend retourne `intervention_type` mais le frontend lit `d.type`. Toujours vérifier le nom exact du champ retourné par l'API avant de le mapper côté frontend

[2026-04-20] | Endpoint `/analytics/technicians` crash avec 500 (Cannot read properties of null) | Des `TechnicianProfile` existent en base sans `User` associé (orphelins). Toujours filtrer `.filter(tech => tech.user)` avant d'accéder aux relations

[2026-04-20] | Endpoint `/analytics/technicians` crash avec `column tp.specialty does not exist` | Le modèle Sequelize référence `specialty` mais la colonne réelle est `specialization`. Toujours vérifier les noms de colonnes réels via `information_schema.columns` avant de mapper

[2026-04-20] | `process.env.DATABASE_URL` non défini quand on SSH avec commande inline | Les variables d'env PM2 (ecosystem.config.js) ne sont pas chargées dans un SSH inline. Utiliser l'URL directe ou `source` l'env avant

[2026-04-21] | Dashboard web avec menu, routes React et widgets rapides désalignés | Quand un onglet est ajouté ou restreint, vérifier systématiquement l'alignement complet menu sidebar + routes App.tsx + liens internes/widgets pour éviter collisions et bypass d'accès

[2026-04-21] | Filtre de dates Analytics corrigé seulement côté page mais ignoré par le service et le backend des graphiques | Pour tout filtre transverse, valider la chaîne complète UI → service frontend → endpoint backend avant de considérer le bug corrigé

[2026-04-21] | Les spies posés après import ne pilotent pas les hooks UI déjà capturés au chargement du module (`useBreakpoint` par exemple) | Pour tester un composant qui capture un hook à l'import, mocker le module au niveau `jest.mock(...)` avant l'import du composant plutôt que d'utiliser un spy runtime

[2026-04-21] | Pages dashboard créées rapidement avec `import axios from 'axios'` et URLs construites à la main (`process.env.REACT_APP_API_URL`) — cassées en prod si env manquante ou sur domaine séparé | Toujours importer l'instance `api` from `../services/api` qui a baseURL + intercepteur token configurés. Ne jamais construire des URLs API manuellement dans les pages.

[2026-04-21] | `Promise.allSettled` dans DashboardStats remplace chaque source tombée par 0 — zéros valides masquent les pannes partielles d'API | Quand on utilise Promise.allSettled, distinguer `null` (erreur) de `0` (vraie valeur) dans les résultats, et propager le flag error dans le state pour l'affichage

[2026-04-21] | Fonctionnalité commentée (`auto-assign`) laisse une modal sans aucun bouton d'action — cul-de-sac UX invisible à la review | Quand une feature est désactivée, soit la remplacer par quelque chose d'utile (comme des boutons Assigner par technicien), soit retirer le point d'entrée dans l'UI jusqu'à ce qu'elle soit prête

[2026-04-21] | API utilisait SQLite en production alors que PostgreSQL était installé et disponible — DATABASE_URL manquante dans le .env | Après tout déploiement, vérifier que DATABASE_URL est présent dans le .env de prod. La config `database.js` bascule silencieusement sur SQLite si DATABASE_URL est absent, sans aucune erreur au démarrage

[2026-04-21] | `!` dans un mot de passe provoque `zsh: event not found` dans les commandes SSH inline | Pour toute commande SSH contenant des caractères spéciaux (`!`, `$`, `@`), toujours passer par un script local envoyé via `scp` puis exécuté à distance, jamais inline

[2026-04-21] | Colonne mot de passe dans la table `users` s'appelle `password_hash` et non `password` — UPDATE échoue silencieusement ou avec erreur | Toujours vérifier les noms de colonnes réels via `information_schema.columns` avant tout UPDATE direct en base

[2026-04-21] | Script Node.js exécuté depuis `/tmp` ne trouve pas les `node_modules` de l'API | Toujours copier les scripts utilitaires dans le dossier racine de l'API (qui contient `node_modules`) avant de les exécuter

[2026-04-21] | Flutter : `badCertificateCallback = (cert, host, port) => true` laissé en dur désactive SSL en production | Conditionner toujours ce callback à `kDebugMode`. En release, ne jamais accepter de certificats invalides

[2026-04-21] | Flutter : deux variables token (`_authToken` SharedPreferences et `_accessToken` mémoire) désynchronisées après redémarrage → toutes les requêtes authentifiées partent sans Bearer token | Un seul token suffit. Si deux références existent, les synchroniser dans `loadSavedToken()` ET dans `setAuthToken()` en même temps

[2026-04-21] | Flutter : `static const bool debugLogs = true` dans `ApiConfig` → logs activés en release | Utiliser `static bool get debugLogs => kDebugMode` pour que les logs ne soient actifs qu'en debug

[2026-04-21] | Flutter : `corsHeaders` définis côté client Flutter et ajoutés aux requêtes HTTP | Les headers CORS (`Access-Control-Allow-*`) sont des headers de réponse serveur — jamais des headers de requête client. Les supprimer complètement du client Flutter

[2026-04-21] | Flutter : token JWT stocké dans `SharedPreferences` (stockage non chiffré) | Stocker les secrets (tokens) dans `FlutterSecureStorage` avec `AndroidOptions(encryptedSharedPreferences: true)`. Garder les données non-sensibles (préférences UI) dans SharedPreferences

[2026-04-21] | Flutter : sur 401, l'app lève une exception sans vider le token → l'utilisateur reste bloqué dans un état authentifié invalide | Sur 401, toujours supprimer le token stocké (`setAuthToken(null)`) avant de lever l'exception, pour forcer une reconnexion propre

[2026-04-21] | Flutter : `api_service_new.dart` duplique `ApiService` avec une interface `http.Response` différente — utilisé seulement par `auth_service.dart` | Ne jamais créer une deuxième instance du service API. Ajouter les méthodes manquantes directement dans le service principal

[2026-04-21] | Flutter : `ErrorWidget.builder` non conditionné expose les stack traces internes en production | Entourer `ErrorWidget.builder` avec `if (kReleaseMode)` pour afficher un message générique en prod et le détail d'erreur seulement en debug

[2026-04-21] | Flutter : `NSBonjourServices/_dartobservatory._tcp` laissé dans `Info.plist` expose le service de debug Dart en build de production iOS | Ce bloc est ajouté automatiquement par les outils de debug Flutter — toujours le retirer de `Info.plist` avant publication App Store

[2026-04-22] | `getInvoices()` appelait `/api/orders` (boutique) et n'affichait que le premier acompte — les devis avec paiement fractionné n'avaient pas de solde visible | Quand plusieurs types de paiements coexistent (commandes boutique + acomptes devis), créer un endpoint d'agrégation dédié plutôt que de réutiliser les endpoints génériques d'orders

[2026-04-22] | Backend DELETE upload refusait les types pluriels ('products', 'equipments') avec 400 alors que le dashboard envoyait le pluriel | Normaliser le paramètre `type` avec `.replace(/s$/, '')` dans le contrôleur pour accepter indifféremment le singulier et le pluriel

[2026-04-22] | POST upload retournait 500 ENOENT car les sous-dossiers `uploads/` n'existaient pas sur le serveur après déploiement | Toujours créer les sous-dossiers d'upload sur le serveur lors de chaque déploiement : `mkdir -p uploads/{products,avatars,equipments,documents,interventions}`

[2026-04-27] | Clé service account Firebase révoquée dans Google Cloud Console → `invalid_grant: Invalid JWT Signature` sur toutes les notifications FCM | Quand FCM retourne `invalid_grant`, vérifier l'état de la clé dans GCP → IAM → Service Accounts → Keys. Générer une nouvelle clé, l'uploader sur tous les serveurs, restart PM2 (Node.js met `require()` en cache — le process doit redémarrer pour recharger le fichier)

[2026-04-27] | Avatar 404 car `CircleAvatar.backgroundImage` n'a pas de fallback sur `child` quand l'image échoue | Toujours utiliser `foregroundImage` (pas `backgroundImage`) dans CircleAvatar quand un child de fallback (initiales) est prévu — Flutter bascule sur `child` automatiquement si foregroundImage échoue

[2026-04-27] | `equipment.imageUrl = filename; equipment.save()` ne persistait rien en DB car le champ `imageUrl` n'était pas dans le modèle Sequelize Equipment | Avant tout `instance.someField = value`, vérifier que le champ est déclaré dans le modèle Sequelize. Sinon, Sequelize l'ignore silencieusement au moment du save()

[2026-04-27] | Images disparaissent après reboot/redeploy car stockées uniquement au filesystem sous `uploads/` dans le dossier app | Pour les images critiques (avatars, équipements), convertir en base64 après compression et stocker le data URL directement en DB (TEXT). Pas de dépendance au filesystem. Taille acceptable pour avatars 400×400@85% (~30-60KB → ~80KB base64)

[2026-04-22] | Prévisualisation image dashboard pointait vers le domaine du dashboard au lieu de l'API — chemin relatif `/uploads/products/xxx.jpg` résolu par le navigateur contre le mauvais domaine | Toujours wrapper les chemins d'upload stockés en base avec `getImageUrl()` (qui préfixe `API_BASE_URL`) lors du chargement depuis `initialValues` — ne jamais supposer qu'un chemin relatif est résolvable dans le contexte navigateur courant

[2026-04-22] | Route spécifique `/export-data` capturée par le catch-all `/:id` avec `authorize('admin')` → 403 | En Express, toujours déclarer les routes spécifiques AVANT les routes `/:param` génériques dans le même fichier

[2026-04-22] | Endpoint Sequelize avec `attributes: [...]` hardcodé → 500 répétés car noms de colonnes inventoriés sans vérification (`total_amount`, `reference`, `plan_name`) | Avant tout `findAll` avec `attributes` manuels, toujours vérifier les champs réels dans le modèle Sequelize correspondant — ne jamais deviner les noms de colonnes

[2026-04-27] | Flutter : export binaire PDF crashait avec `FormatException: Invalid UTF-8 byte` car `_request()` décodait le stream en UTF-8 | Pour télécharger du contenu binaire (PDF, ZIP…), ne jamais passer par `_request()` qui décode en string. Utiliser `_client.send()` directement + `streamed.stream.toBytes()` pour récupérer les octets bruts

[2026-04-27] | Flutter WebView : `_isPaymentSuccessUrl` utilisait `contains('success')` — toute URL avec ce mot déclenchait un faux paiement réussi | La détection d'URL de retour paiement doit toujours être restreinte aux domaines connus (fineopay.com, mct.ci) avec vérification du chemin ET de la query. Utiliser `Uri.tryParse()` et ne jamais faire de matching par mots-clés génériques

[2026-04-27] | Flutter : `int.parse(widget.invoiceId)` crashe si l'ID contient une lettre (ex: ID de démo ou mauvais format API) | Toujours utiliser `int.tryParse()` + guard explicite quand la valeur vient de l'extérieur (widget param, API response). `int.parse()` ne doit s'utiliser que sur des strings dont on contrôle le format

[2026-04-27] | Flutter : polling `Timer.periodic` dans les écrans de paiement sans compteur — tournait indéfiniment si le paiement n'était jamais confirmé | Tout `Timer.periodic` de vérification doit avoir un compteur max (ex: 60 × 5s = 5 min) et annuler la timer + informer l'utilisateur si ce plafond est atteint

[2026-04-27] | Flutter : fallback `_getDemoInvoices()` dans le catch — affichait 4 fausses factures hardcodées en cas d'erreur API | Ne jamais utiliser de données fictives comme fallback d'erreur dans une app de production. Afficher un état d'erreur explicite avec un bouton "Réessayer"

[2026-04-30] | DATABASE_URL sur le serveur pointait vers Supabase alors que la vraie DB est locale (PostgreSQL OVH) — API en crash loop silencieux avec ENOTFOUND | Après tout redéploiement, vérifier immédiatement `pm2 logs --lines 10` — si ENOTFOUND sur DATABASE_URL, l'URL pointe vers un host DNS inexistant. Utiliser `psql "$DATABASE_URL" -c "SELECT 1"` pour tester avant de relancer PM2

[2026-04-30] | `ApiService` importé dans 8 fichiers sans jamais être utilisé — import orphelin impossible à détecter sans grep | Avant de supprimer un fichier, toujours grep `import.*fichier.dart` dans tout le projet et vérifier que chaque import utilise effectivement le fichier (pas juste importé par habitude)

[2026-04-30] | `flutter pub upgrade` ne touche pas les breaking changes — `flutter pub upgrade --major-versions` met à jour les contraintes pubspec automatiquement | Pour une mise à jour complète des packages Flutter, lancer les deux commandes dans l'ordre : 1) `flutter pub upgrade` (mineures/patch) 2) `flutter pub upgrade --major-versions` (majeures) puis valider avec `flutter analyze`

[2026-04-27] | Flutter : `_currentPaymentPhase` retournait 1 même si les deux paiements étaient déjà `paid` — le bouton restait actif et permettait un troisième paiement | Toujours vérifier l'état complet (tous les jalons payés) avant d'activer un bouton de paiement. Ajouter une phase "0 = tout payé" avec affichage explicite et bouton désactivé

[2026-04-27] | Flutter : `subscription_payment_screen.dart` retournait un faux succès (dialog + `return true`) quand `checkoutUrl` était null | Quand un paramètre critique est absent (URL de paiement, ID transaction…), toujours lever une exception — jamais simuler un succès silencieux

[2026-04-27] | Flutter : URL ngrok et IPs LAN hardcodées dans `environment.dart` committées dans le dépôt Git | Les fichiers de configuration locale (IPs, ngrok, secrets) doivent être dans `.gitignore` dès le premier commit. Vider les valeurs par défaut (chaîne vide) pour forcer la configuration locale explicite

[2026-04-27] | Flutter : `_saveCart()` appelée sans `await` ni `unawaited()` dans CartService — intent ambigu, lint warning | Quand un appel async est volontairement fire-and-forget, wrapper avec `unawaited()` de `dart:async` pour déclarer l'intent explicitement et supprimer le warning

[2026-04-27] | Flutter : `Color.from(alpha:1, red:0.933, green:0.741, blue:0.106)` utilisé à la place de `Color(0xFFEEBD1B)` — notation non standard dans un projet qui utilise partout `0xFFxxxxxx` | Toujours utiliser la notation `Color(0xFFRRGGBB)` dans Flutter pour la cohérence. `Color.from()` avec flottants linéaires n'est utile que pour les espaces de couleur non-sRGB

[2026-04-28] | Refactoring massif d'une God Class (`ApiService`) casse l'application entière si supprimée d'un coup | Découper la classe en sous-repositories (`AuthRepository`, `InterventionRepository`), mais conserver l'ancienne classe et migrer les appels de manière incrémentale. Ne supprimer la God Class qu'à la toute fin.

[2026-04-28] | Déplacement de dizaines de fichiers dans une nouvelle architecture Feature-First brise tous les imports relatifs dans un projet Flutter | Utiliser un script d'automatisation (Python/Bash) ou un outil de refactoring de masse pour rechercher et remplacer les anciens chemins par les nouveaux dans tout le dossier `lib/` afin d'éviter des heures de travail manuel sujet aux erreurs.

[2026-04-28] | `if (!mounted) return;` inséré par erreur à l'intérieur des listes d'arguments de widgets (ex: `onPressed: () { if (!mounted) return; ... }`) → casse la syntaxe | Toujours vérifier le contexte d'insertion des guards `mounted`. Ils doivent être la première instruction d'un bloc `{}` après un `await`, jamais au milieu d'une expression.

[2026-04-28] | `activeThumbColor` utilisé sur `Radio` ou `CheckboxListTile` provoque une erreur `undefined_named_parameter` | `activeThumbColor` est spécifique à `Switch`. Pour `Radio` et `Checkbox`, utiliser `activeColor` (ou `fillColor` avec `WidgetStateProperty`).

[2026-04-28] | Widget tests échouent car ils référencent `MyApp` (nom par défaut) alors que l'application s'appelle `App` | Toujours vérifier le nom exact de la classe racine dans `main.dart` avant de configurer les tests.

[2026-04-28] | Script d'automatisation des accolades (`{}`) insère des accolades au milieu de structures complexes (collection-if Flutter) → casse la syntaxe (6000+ erreurs) | Ne jamais utiliser de simple script regex pour insérer des accolades dans du code Flutter riche en Widgets. Utiliser `dart format` ou des outils de refactoring officiels pour les lints de style, ou faire des passes manuelles ciblées.

[2026-04-28] | Migration Feature-First : imports de modèles/services partagés oubliés dans le script de migration global | Pour un refactoring d'architecture, construire une liste exhaustive des fichiers "partagés" (services, modèles, providers) et s'assurer que le script de remplacement d'imports couvre tous les fichiers déplacés, pas seulement les écrans.

[2026-04-28] | `context.mounted` versus `mounted` | Dans les builders ou dialogues, `mounted` (de l'état State) peut être vrai alors que le `context` local est déjà invalidé. Utiliser `if (context.mounted)` pour plus de précision.

[2026-04-28] | Script d'ajout d'imports `foundation.dart` échoue si un import partiel existe déjà (ex: `show kReleaseMode`) | Vérifier non seulement la présence du package mais aussi s'il est restreint. Si `show` est présent, il vaut mieux passer à un import complet pour supporter `kDebugMode` et `debugPrint`.

[2026-04-29] | `Geolocator.getCurrentPosition` utilisant `desiredAccuracy` (déprécié) | Utiliser `locationSettings` (LocationSettings) avec le paramètre `accuracy` pour se conformer aux versions récentes de Geolocator.

[2026-04-29] | Migration vers le pattern Repository : Oubli d'ajouter les méthodes dans l'interface/implémentation du Repository après les avoir utilisées dans l'UI | Toujours mettre à jour l'interface `abstract class` et son `Impl` AVANT ou EN MÊME TEMPS que la migration de l'écran UI. Lancer un build de test après chaque écran migré.

[2026-04-29] | Récursion infinie dans `SyncProvider` due au retry automatique de 10s sur échec | Ne jamais planifier de retry automatique à court terme indéfini à l'intérieur d'une méthode de sync globale. Préférer les timers périodiques longs (ex: 5 min) ou les déclencheurs d'événements (connectivité) pour éviter les boucles infinies en cas d'erreur persistante.

[2026-04-29] | Singletons créant des abonnements multiples à chaque `initialize()` | Toujours vérifier si un abonnement existe déjà (`_subscription != null`) avant de s'abonner à un stream dans une méthode d'initialisation répétable.

[2026-04-29] | Backend forçant le stockage Base64 au lieu des fichiers | Dans `uploadController.js`, l'utilisation systématique de `fileToBase64DataUrl` supprimait les fichiers physiques après upload. Toujours préférer le stockage de fichiers physiques pour les avatars et équipements afin de préserver la performance de la base de données et du réseau.

[2026-07-09] | API et App : Ajout de champs de mesure technique (pression, puissance, etc.) | Au lieu d'utiliser un champ générique "technical_data", définir des colonnes de mesure spécifiques et cohérentes (Pression, Puissance, Intensité, Tension) entre les modèles Flutter et les modèles Sequelize pour éviter les conversions de données difficiles.

[2026-07-09] | API et App : Ajout du Fréon | Ajout de la mesure de Fréon dans le rapport de diagnostic et le rapport de maintenance.

[2026-07-09] | Xcode Cloud & Flutter | Ajout du script ci_post_clone.sh pour forcer Xcode Cloud à installer Flutter et exécuter 'pod install' avant de tenter la compilation iOS, car par défaut Xcode Cloud ne gère pas nativement Flutter.

[2026-07-09] | Sequelize et Enums Postgres | PostgreSQL est très strict avec les types ENUM. `sequelize.sync({ alter: true })` ne rajoute pas toujours les nouvelles valeurs d'un ENUM (ex: `intervention_status` ou `notifications.type`). Il faut faire des `ALTER TYPE ... ADD VALUE` manuellement en base ou écrire des migrations explicites ou utiliser une valeur existante.

[2026-07-09] | ID Utilisateur vs ID Profil | Il faut toujours vérifier si une table fait référence à l'ID `User` (authentification) ou l'ID `CustomerProfile`. Dans l'API, `Intervention.customer_id` pointe sur `CustomerProfile.id`, donc utiliser `req.user.id` directement dans les clauses `where` provoque des erreurs 404 (non trouvé). Toujours faire la conversion via une requête préalable (`CustomerProfile.findOne`).

[2026-07-10] | Notifications "alert" et navigation | Le type de notification `alert` était codé en dur pour rediriger vers le profil utilisateur (prévu initialement pour les adresses manquantes). Lorsqu'on l'utilise pour une alerte d'intervention (ex: technicien sur place injoignable), cela causait une mauvaise redirection. Il faut vérifier la présence d'un `relatedId` ou `interventionId` dans le payload pour faire un routage conditionnel.

[2026-07-10] | Le tableau de bord n'affichait pas les modifications de code même après avoir vidé le cache et utilisé rsync pour transférer le dossier build vers le VPS | Les configurations Nginx (comme `/etc/nginx/sites-available/smartmaintenance`) peuvent pointer vers un sous-dossier inattendu pour un domaine donné. Toujours vérifier la directive `root` (ex: `root /var/www/smartmaintenance/mct-maintenance-dashboard/build;`) avant de copier les fichiers à l'aveugle dans ce qui semble être le dossier logique (ex: `dashboard/`).

[2026-07-13] | Erreur `Unexpected field` (MulterError) levée par l'API Node.js lors de la soumission d'un formulaire multipart/form-data par Flutter | Le middleware `multer` est très strict sur le nom des clés contenant des fichiers uploadés. Si la route backend déclare `upload.array('images')`, envoyer un fichier avec la clé `photos` (`http.MultipartFile.fromPath('photos', path)`) provoquera systématiquement un crash "Unexpected field". Toujours vérifier que la clé du FormData (côté Frontend) correspond exactement au champ attendu par Multer (côté Backend).

[2026-07-13] | Crash du rendu Flutter avec `Image.file()` lors de la sélection d'une vidéo | Le widget `Image.file()` dans Flutter essaie de décoder l'en-tête du fichier pour l'afficher comme une image. S'il reçoit un fichier vidéo (.mp4, .mov), il lèvera une exception de décodage. Lors de l'implémentation d'une sélection mixte (photos/vidéos), toujours vérifier l'extension du fichier (ex: `path.endsWith('.mp4')`) avant de rendre le composant de prévisualisation, et afficher une icône ou une miniature alternative pour les vidéos.

[2026-07-13] | Erreur 500 sur le serveur de production lors de l'envoi d'une vidéo (alors que ça marchait en local) | Toute modification des règles backend (comme autoriser le format .mp4 dans la configuration `multer`) doit impérativement être synchronisée et déployée sur le serveur distant (VPS) avec `rsync` puis un `pm2 restart`, sinon l'API de production continuera d'utiliser l'ancien code et rejettera la vidéo. Toujours déployer le backend avant de tester le frontend.

[2026-07-13] | Rejet de fichier par Multer pour les vidéos provenant de l'appareil photo iOS (.MOV) | Lors de l'enregistrement avec la caméra sur iOS, le fichier généré a l'extension `.mov` mais son `mimetype` est `video/quicktime`. Si on utilise une Regex dans `multer` pour le mimetype comme `/jpeg|jpg|png|gif|mp4|mov|avi/`, le type `video/quicktime` ne matchera pas (car il ne contient pas le mot `mov` !). Il faut explicitement autoriser `video/quicktime` dans les mimetypes acceptés.

[2026-07-15] | SocketException lors de la récupération des données en mode hors-ligne pour le technicien | Si l'application doit supporter le mode hors-ligne, vérifier que TOUS les endpoints de l'API appelés (pas seulement ceux côté client, mais aussi ceux côté technicien) intègrent une condition `if (!_connectivityService.isConnected)` avec un accès correspondant au cache local. Un oubli entraîne un plantage de connexion (SocketException/ClientException) au lieu d'une lecture propre du cache.

[2026-07-15] | Faux positifs de connectivité | La vérification de connectivité via `connectivity_plus` ne garantit pas que le serveur est joignable. Il faut TOUJOURS encadrer les appels réseaux (ex: `_apiService.get`) avec un `try/catch` pour faire un fallback vers le cache SQLite en cas de `SocketException`.

[2026-07-15] | Surcharge manuelle du thème global (bordures carrées au lieu de rondes) | Lorsque l'on définit un thème global comme `InputDecorationTheme(borderRadius: BorderRadius.circular(16))` dans `themes.dart`, toute déclaration `border: OutlineInputBorder()` codée en dur dans les `TextFormField` écrase le thème global et réinitialise la bordure à son apparence carrée par défaut. Il faut supprimer les attributs `border`, `filled`, `fillColor` des `InputDecoration` locaux pour laisser le design global s'appliquer de façon harmonieuse.

[2026-07-15] | Mise à jour en direct (live) des champs de texte ignorée | Si un élément d'interface (comme le titre dynamique d'une carte d'équipement) dépend de la saisie d'un champ texte, le simple fait de faire `_equipments[index]['name'] = value` dans le `onChanged` ne mettra pas à jour l'UI. Il faut impérativement encapsuler l'affectation dans un `setState(() { ... })` pour déclencher le rafraîchissement "en direct" pendant la frappe au clavier.

[2026-07-15] | Exception Floating SnackBar presented off screen | Dans un écran complexe (ex: avec `Scaffold`, `BottomNavigationBar`, `FloatingActionButton` et des `Scaffold` imbriqués), utiliser un `SnackBarBehavior.floating` avec des marges globales (`EdgeInsets.all(16)`) peut déclencher une assertion car Flutter considère qu'il n'y a pas assez de place verticale pour repousser le `SnackBar` au-dessus de tous ces éléments. Solution : utiliser `SnackBarBehavior.fixed` sans `margin` ou appliquer des marges spécifiques (`EdgeInsets.only(left, right, bottom)`) si on veut absolument qu'il flotte, tout en tenant compte de la SafeArea.

[2026-07-15] | Animation de transition personnalisée ignorée par MaterialPageRoute | `MaterialPageRoute` et `CupertinoPageRoute` gèrent leurs propres animations natives (généralement du bas vers le haut, ou de la droite vers la gauche sur iOS). Pour forcer une navigation glissante personnalisée (ex: de la gauche vers la droite), il ne faut pas utiliser `MaterialPageRoute`. Il faut utiliser un `PageRouteBuilder` et paramétrer son `transitionsBuilder` avec un `SlideTransition` basé sur un `Offset(-1.0, 0.0)` (gauche vers droite).

[2026-07-15] | Erreur de syntaxe inattendue lors de l'intégration de Widget wrapper | Lors de l'enveloppement d'un gros bloc de code Flutter existant avec un nouveau parent (ex: `Container` encapsulé dans `Padding` > `Dismissible`), les remplacements automatiques ou regex risquent souvent de décaler les parenthèses de fermeture, causant des erreurs `Expected ')'` distantes de la zone de modification (en fin de fonction). Préférer faire le remplacement précis des blocs d'ouverture et de fermeture avec un outil de remplacement multiple, ou confier la manipulation à un IDE, plutôt que d'utiliser des injections textuelles simplistes.

[2026-07-16] | sequelize.sync({alter: true}) echoue souvent sur SQLite lorsqu'il y a des changements de colonnes ou contraintes uniques sur d'autres tables non liees (ex: users). | Regle: Utiliser des ALTER TABLE natifs (via sqlite3 CLI ou raw queries) pour modifier le schema de production sur SQLite plutot que d'appeler sync({alter: true}) qui recrée les tables de maniere instable.

[2026-07-16] | Le serveur Socket.io n'est pas accessible via le sous-chemin /api | Lorsque l'on configure `socket.io` côté Node.js avec les paramètres par défaut, il s'attache à la racine du serveur HTTP (souvent `/`). Même si nos requêtes REST passent par un proxy `/api`, l'URL de connexion du client `socket.io-client` doit pointer vers la racine du serveur, sans le `/api` à la fin, sous peine d'erreurs de connexion et requêtes bloquées.

[2026-07-16] | Filtre strict sur la disponibilité pour les suggestions d'assignation | Il ne suffit pas de vérifier si un utilisateur a le statut "actif" (compte non suspendu). L'algorithme d'assignation doit impérativement filtrer la colonne d'état de disponibilité en temps réel (`availability_status`). Un technicien "occupé" ou "hors ligne" ne doit pas être suggéré pour une urgence immédiate, sinon cela fausse les propositions de planification.

[2026-07-16] | Problèmes de permissions (403) sur de nouvelles routes | L'ajout d'une fonctionnalité comme la géolocalisation des techniciens demande souvent un point de terminaison "mixte" : les techniciens écrivent leur position, mais les admins/agents/managers doivent pouvoir la lire. Veillez à ne pas placer de telles routes sous un middleware global restrictif (ex: `authorize('technician')`). Placez-les avant, avec des autorisations étendues.

### 🚀 Méthodologie de Déploiement en Production (VPS 77.42.22.25)
*(Toujours vérifier que le SSH depuis la machine de dev n'est pas bloqué par un pare-feu)*
- **Déploiement Backend (API)** :
  ```bash
  rsync -avz /Users/bassoued/Documents/MAINTENANCE/mct-maintenance-api/src/ root@77.42.22.25:/var/www/smartmaintenance/mct-maintenance-api/src/
  ssh root@77.42.22.25 "pm2 restart smartmaintenance-api"
  ```
- **Déploiement Frontend (Dashboard)** :
  ```bash
  cd /Users/bassoued/Documents/MAINTENANCE/mct-maintenance-dashboard
  npm run build
  rsync -avz build/ root@77.42.22.25:/var/www/smartmaintenance/mct-maintenance-dashboard/build/
  ```

[2026-07-16] | Assignation Automatique manquante après paiement diagnostic | Ne pas oublier de lier le paiement du webhook ('diagnostic_paid') à l'auto-assignation, et de mettre à jour les filtres du Cron (statut 'pending' incluant les interventions payées).

[2026-07-17] | iPhones XR/X (iOS 15/16) ne recevaient pas les notifications push alors que les plus récents oui | 1) Sur le backend, pour les payloads HTTP v1 FCM vers iOS, il faut impérativement inclure 'apns-priority': '10' et 'content-available': 1 dans la payload APNs sinon Apple rejette silencieusement sur les anciens OS. 2) Sur l'app mobile, dans AppDelegate.swift, il ne faut PAS affecter UNUserNotificationCenter.current().delegate = self manuellement sans transférer (super) les callbacks, car ça écrase la configuration du plugin flutter_local_notifications et empêche firebase_messaging de récupérer le token APNs sur les anciens OS.

[2026-07-18] | Les évaluations des techniciens semblaient disparaître et les commentaires textuels étaient absents de l'API | 1) Dans `reviews_screen.dart` (Mobile Technicien), la liste `_reviews` était chargée en état mais jamais passée aux enfants du `ListView` (la méthode de rendu `_buildReviewCard` n'était jamais appelée). 2) Côté Backend (`technicianRoutes.js`), la route `GET /api/technicians/reviews` omettait le champ `review` (commentaire textuel) et la relation `customer` (nom du client) dans le JSON retourné. 3) Côté Admin (`adminRoutes.js`), le mapping des techniciens n'incluait pas `rating` et `total_reviews` depuis `TechnicianProfile`. Toujours vérifier que toute la chaîne (Requête DB -> DTO Backend -> Widgets UI) contient et rend la liste d'éléments complète.

[2026-07-18] | Crash Android `MissingForegroundServiceTypeException: Starting FGS without a type` et Conflit Manifest Merger | Depuis Android 14 (API 34+ / targetSDK >= 34), tout Foreground Service (`flutter_background_service`) doit obligatoirement avoir son type spécifié dans `AndroidManifest.xml` via `android:foregroundServiceType="location"` dans la balise `<service>` sous `<application>`. De plus, il faut déclarer `xmlns:tools="http://schemas.android.com/tools"` sur la balise `<manifest>` et ajouter `tools:replace="android:exported"` sur la balise `<service>` pour éviter le conflit lors de la fusion du Manifeste Gradle avec le package plugin (`flutter_background_service_android`).

[2026-07-20] | Erreur 500 sur /api/maintenance-offers en production (column "price_note" does not exist) car la migration DB a été faite en local sur SQLite mais pas sur la base PostgreSQL du serveur VPS | Toujours vérifier quel SGBD est configuré dans le .env du VPS (PostgreSQL vs SQLite) et exécuter la commande `ALTER TABLE ... ADD COLUMN IF NOT EXISTS ...` via psql sur PostgreSQL (`smartmaintenance_db`) lors de tout ajout de colonne sur un modèle Sequelize.

[2026-07-20] | Rejet App Store / TestFlight 409 (Invalid executable objective_c.framework references an unsupported platform in arm64 slice) | Apple interdit tout code compilé pour simulateur dans les paquets Release/App Store. Pour corriger, ajouter dans le `post_install` du `ios/Podfile` l'exclusion des architectures simulateur : `if config.name == 'Release'` -> `config.build_settings['ONLY_ACTIVE_ARCH'] = 'YES'` et `config.build_settings['EXCLUDED_ARCHS[sdk=iphonesimulator*]'] = 'arm64 i386 x86_64'`.

[2026-07-23] | RenderFlex overflow (horizontal) sur l'affichage du titre de l'offre dans mct_maintenance_mobile | Lors de l'affichage d'un titre dynamique d'une offre (ex: "Offre ESSENTIEL à la demande") dans une `Row`, si le texte est trop long, il déborde de l'écran car la `Row` tente de dimensionner le widget `Text` à sa largeur naturelle. Toujours envelopper le widget `Text` dans un widget `Expanded` (ou `Flexible`) lorsqu'il est imbriqué dans une `Row` ou un `Flex` horizontal afin de permettre un retour à la ligne automatique et prévenir tout overflow.

[2026-07-27] | Xcode ne trouvait aucune destination simulateur ("Unable to find a destination matching...") lors du flutter run sur Mac Apple Silicon (M1+) | La clause `EXCLUDED_ARCHS[sdk=iphonesimulator*] = 'arm64 ...'` présente dans le Podfile excluait `arm64` des simulateurs. Or sur Mac Apple Silicon (M1/M2/M3/M4/M5), les simulateurs fonctionnent exclusivement en `arm64`. Ne jamais appliquer `EXCLUDED_ARCHS` sur `arm64` pour `iphonesimulator` dans le `Podfile`, et s'assurer que `SUPPORTED_PLATFORMS = "iphonesimulator iphoneos";` est présent dans `Runner.xcodeproj`.

[2026-07-27] | Erreur `type 'Null' is not a subtype of type 'String'` lors du chargement des services de réparation ou d'installation sur mobile | Les modèles Flutter (`RepairService` et `InstallationService`) déclaraient `final String model;` obligatoire sans null-safety. Quand un service n'a pas de modèle/type renseigné en base de données (`model: null`), `fromJson` levait une exception au parsing. Toujours déclarer les champs textuels optionnels de la BDD comme `final String? model;` (nullable) dans les modèles Dart, et protéger l'affichage UI avec un guard `if (service.model != null && service.model!.isNotEmpty)`.

[2026-07-27] | L'écran de paiement affichait "Acompte entretien (50%)" et "Acompte de 50% requis" même lorsque l'utilisateur sélectionnait l'option 100% de paiement | `DiagnosticPaymentScreen` ne recevait pas le paramètre `paymentOption` ('split' vs 'full') depuis `new_intervention_screen.dart`. Toujours transmettre l'option de paiement sélectionnée (`paymentOption: _maintenancePaymentOption`) et adapter dynamiquement le titre ("Paiement entretien (100%)") et les notes explicatives ("Paiement intégral (100%) - Aucun solde restant") selon le choix du client.

[2026-07-27] | Redirection indésirable vers l'écran d'accueil après création/paiement d'une intervention ou souscription | 1) En cas d'échec, d'annulation ou de timeout du paiement (`DiagnosticPaymentScreen`, `ContractPaymentScreen`), ne pas dépiler l'écran vers l'accueil (`Navigator.pop(context, false)`), mais maintenir l'écran de paiement ouvert avec le message explicatif et le bouton de paiement disponible pour réitération. 2) En cas de succès d'une création d'intervention, rediriger vers `InterventionDetailScreen` avec `Navigator.pushReplacement` au lieu de dépiler vers l'accueil, afin que l'utilisateur accède immédiatement à la fiche de son intervention créée.

[2026-07-27] | Le rapport affiché après les travaux/maintenance était identique au rapport de diagnostic initial | Lors de l'ouverture de `create_report_screen.dart` par le technicien, le formulaire pré-remplissait la description et les mesures depuis `widget.intervention['report_data']` qui contenait les informations du diagnostic. 1) Dans `create_report_screen.dart`, si `report_data` provient d'un diagnostic (`report_type == 'diagnostic'`), conserver la liste des équipements tout en réinitialisant la description des travaux et les mesures post-maintenance à zéro. 2) Taguer le rapport soumis avec `'report_type': 'after_work'`. 3) Dans `InterventionDetailScreen` (côté client), titrer et structurer distinctement le "Rapport de Diagnostic" (problème identifié) et le "Résumé des travaux effectués" (maintenance réalisée).

[2026-07-27] | Certains techniciens omit volontairement de se mettre en ligne dans l'application mobile | Ajout de la capacité pour le Back-Office (Admin/Manager) de modifier le statut de disponibilité des techniciens directement depuis le tableau de bord web (`TechniciansPage.tsx`). 1) Côté Backend (`technicianController.js`, `adminRoutes.js`, `technicianRoutes.js`), création de la méthode `updateTechnicianAvailabilityByAdmin` accessible via `PATCH /api/admin/technicians/:id/availability` ou `PATCH /api/technicians/:id/availability`, qui met à jour `TechnicianProfile.availability_status`, émet un événement Socket.io (`technician_status_changed`) et notifie le technicien via notification Push. 2) Côté Dashboard (`TechniciansPage.tsx`), ajout d'une colonne de sélection interactive "Disponibilité (En ligne)" avec options visuelles (🟢 En ligne, 🟡 En intervention, ⚪ Hors ligne) permettant de commuter le statut d'un technicien en 1 clic.

[2026-07-27] | Erreur 500 / 404 lors de l'appel d'API du Dashboard avec URL /api/api/... dupliquée | Dans le Dashboard React, `api.ts` définit déjà la `baseURL` à `https://api.sandbox.mct.ci/api`. Les méthodes HTTP dans les services (`techniciansService.ts`) ne doivent pas inclure le préfixe `/api` (ex: `/admin/technicians/${id}/availability` au lieu de `/api/technicians/...`), sinon Axios concatène pour produire `/api/api/...` qui échoue.

[2026-07-28] | Erreur 404 sur le chargement des avatars de techniciens dans le Dashboard | Les avatars sous forme de nom de fichier simple (`avatar-40-1777293329555.jpg`) étaient demandés par le navigateur sur le domaine web du dashboard (`https://dashboard.sandbox.mct.ci/avatar-...`) au lieu du serveur d'API (`api.sandbox.mct.ci`). 1) Dans `uploadService.ts`, mise à jour de `getImageUrl` pour mapper automatiquement les noms de fichiers vers `${API_BASE_URL}/uploads/avatars/`. 2) Dans `TechnicianMap.tsx`, passer `tech.profile_image` à `getImageUrl(...)` pour la carte et le popup Leaflet.

[2026-07-28] | Erreur 500 Sequelize lors de la création d'une notification avec un type hors ENUM | Dans le modèle Sequelize `Notification.js`, le champ `type` est un type ENUM restreint. L'utilisation d'une valeur personnalisée comme `'availability_changed_by_admin'` provoquait une erreur de validation BDD 500. Remplacement par la valeur ENUM valide `'alert'` dans `technicianController.js`.

[2026-07-28] | Disponibilité technicien réinitialisée à 'offline' lors de l'actualisation du dashboard | L'endpoint `GET /api/users` (`userController.js:listUsers`) ne joignait pas le modèle `TechnicianProfile`. Par conséquent, `availability_status` n'était pas renvoyé dans la liste des utilisateurs, provoquant un fallback visuel vers 'offline' lors d'un rafraîchissement de page. Solution : inclusion de `TechnicianProfile` dans `listUsers` et `getUser` pour associer `availability_status` à chaque profil utilisateur.

[2026-07-28] | La colonne Disponibilité du tableau techniciens ne se mettait pas à jour en temps réel depuis le mobile | La carte interactive `TechnicianMap` écoutait les événements Socket.io `technician_status_changed`, mais la page `TechniciansPage.tsx` ne mettait pas à jour son état React. Solution : propagation de l'événement dans `socketService.ts` et abonnement temps réel dans `TechniciansPage.tsx` pour répercuter le changement dans le tableau instantanément.

[2026-07-28] | Impossibilité de passer Souleymane Fofana en ligne & erreur notNull Violation sur TechnicianProfile | 1) Le compte de Souleymane Fofana (ID 81) était configuré en base avec `role: 'customer'` au lieu de `'technician'`. Son rôle a été corrigé vers `'technician'` et son profil `TechnicianProfile` a été créé. 2) Dans `technicianController.js`, la création dynamique d'un profil technicien manquant extrait désormais `first_name`, `last_name` et `phone` de l'utilisateur pour éviter les violations `notNull` de Sequelize.

[2026-07-28] | Ajout d'une balise et d'un indicateur de statut de disponibilité sur l'accueil mobile technicien | Sur `TechnicianMainScreen`, intégration d'une balise interactive (Chip) directement dans la carte d'accueil ("Bonjour Technicien !") affichant 🟢 En ligne, 🟡 Occupé, ⚪ Hors ligne. Cliquer sur la balise ouvre directement l'écran de modification des disponibilités. Ajout également d'un point de couleur sur l'avatar du technicien dans la barre d'en-tête (AppBar).

[2026-07-28] | Connection Socket.io automatique & Gestion des erreurs d'image Avatar 404 sur Mobile | 1) `ChatService().connect()` est désormais automatiquement appelé dès le chargement du tableau de bord technicien (`_loadDashboardData()`), garantissant que le socket reste connecté pour recevoir l'événement `technician_status_changed` en temps réel. 2) Ajout de `onForegroundImageError: (_, __) {}` sur les `CircleAvatar` afin de gérer proprement les images 404 sans bloquer l'interface et afficher la lettre initiale par défaut.

[2026-07-28] | Redirection des notifications de disponibilité vers l'Accueil au lieu du Profil | Dans `NotificationNavigationService.dart`, lors d'un clic sur une notification de type `alert` ou `availability_changed` dont le titre contient « disponibilité », la navigation dépile jusqu'à la première route (`popUntil((route) => route.isFirst)` - l'Accueil du tableau de bord) au lieu de rediriger vers l'écran de profil.

[2026-07-28] | Transmission de title et message dans notificationData pour la navigation | Dans `technician_notifications_screen.dart` et `notifications_screen.dart`, les champs `title` et `message` ont été ajoutés à la map `notificationData` afin de permettre à `NotificationNavigationService` de détecter correctement les notifications de disponibilité et de rediriger l'utilisateur vers l'accueil (`TechnicianMainScreen` / `CustomerMainScreen`).

[2026-07-28] | Correction majeure du Callback FineoPay & Rapprochement des paiements restés en attente | 1) `API_BASE_URL` dans le `.env` backend contenait une IP locale (`192.168.1.139`), empêchant les webhooks FineoPay de valider les paiements. Remplacement par `https://api.sandbox.mct.ci` et passage à `FINEOPAY_ENV=production`. 2) Ajout du helper `getCallbackUrl()` dans `fineoPayController.js` garantissant l'utilisation de l'URL publique. 3) Exécution d'un script de rapprochement ayant validé le paiement des diagnostics et assigné les interventions (#194 à #199) prélevées hier.

[2026-07-28] | Correction d'assertion CircleAvatar & SnackBar dans Flutter | Dans Flutter, `CircleAvatar` exige `assert(foregroundImage != null || onForegroundImageError == null)`. Passer `onForegroundImageError: AvatarHelper.hasAvatar(...) ? (_, __) {} : null` résout le crash lorsque l'utilisateur n'a pas d'avatar. De plus, l'appel à `SnackBarHelper.showError` en cas de coupure réseau est désormais protégé par un bloc `try/catch` pour éviter l'assertion `_scaffolds.isNotEmpty`.

[2026-07-28] | Détection des coordonnées GPS par défaut de l'émulateur Android (San Jose) | Lorsqu'un technicien se connecte depuis un émulateur Android, `Geolocator` renvoie la position GPS simulée par défaut du SDK Android (`37.4219983, -122.084` San Jose / Mountain View, Californie). Ajout d'une sécurité dans `technicianController.js` qui détecte ces coordonnées par défaut et les remappe automatiquement vers Abidjan, Côte d'Ivoire (`5.359951, -4.008256`).

[2026-07-28] | Design de la carte d'accueil technicien dynamique selon le statut de disponibilité | Refonte de `_buildWelcomeCard` dans `technician_main_screen.dart` avec un `AnimatedContainer` dont la bordure (épaisseur 2.2px), le dégradé d'icône et l'ombre portée (boxShadow) s'adaptent instantanément et dynamiquement à la couleur de l'état (Vert Émeraude pour En ligne, Orange pour Occupé, Gris pour Hors ligne).

[2026-07-28] | Type de Fréon & Galerie Photo des rapports d'intervention | 1) Remplacement du champ Fréon (Kg) par la saisie du Type de Fréon (ex: R410, R22, R32, R134a) dans les formulaires de rapport diagnostic/maintenance et suppression du suffixe forcé "Kg" sur toutes les vues d'affichage client et technicien. 2) Ajout d'une galerie photo interactive avec aperçu grand écran zoomable (InteractiveViewer) dans `view_report_screen.dart`, `view_diagnostic_report_screen.dart` et `intervention_detail_screen.dart` permettant aux clients de visualiser directement les photos téléversées par les techniciens.

[2026-07-28] | Réception des demandes d'intervention sur le Dashboard uniquement APRÈS paiement | 1) Dans `interventionController.js` (`createIntervention`), la notification aux admins (`notifyAdmins`) est désormais reportée pour les demandes clients payantes (`!isFreeDiagnosis && diagnosticFee > 0 && !diagnostic_paid`). Seules les demandes gratuites ou souscrites envoient une notification immédiate. 2) Dans `getAllInterventions` (`GET /api/interventions`), un filtre automatique masque les demandes clients impayées du tableau de bord général. Dès que la confirmation de paiement est reçue via le webhook FineoPay (`fineoPayController.js`), `diagnostic_paid` passe à `true`, la notification admin est émise et l'intervention apparaît instantanément sur le Dashboard. 3) Les statistiques du Dashboard (`pendingAssignment`, `interventionsToday` dans `adminRoutes.js`) excluent également les demandes impayées.

[2026-07-28] | Autorisation du prix 0 FCFA pour les offres d'entretien à des fins de test | Dans `maintenanceOfferRoutes.js`, conversion explicite de `price` via `Number(price)` et remplacement des vérifications `!price` qui rejetaient `0` (car `!0 === true`). La création et la modification d'offres d'entretien avec un prix de `0 FCFA` fonctionnent désormais parfaitement sans erreur 400 Bad Request.

[2026-07-28] | Chemin de déploiement VPS exact pour l'API Backend | Le dossier cible de déploiement de l'API sur le serveur VPS est `/var/www/smartmaintenance/mct-maintenance-api/` (et non `/var/www/mct-maintenance-api/`). Toujours exécuter `rsync` vers `/var/www/smartmaintenance/mct-maintenance-api/` suivi de `pm2 reload smartmaintenance-api` pour que l'application en cluster prenne immédiatement en compte le nouveau code.

[2026-07-28] | Auto-validation du statut de paiement pour les souscriptions et interventions à 0 FCFA | Puisque les agrégateurs de paiement (CinetPay/FineoPay) ne peuvent pas traiter de transactions à 0 FCFA, toute souscription ou intervention créée avec un montant de `0 FCFA` (offre gratuite/test ou tarif 0) passe désormais automatiquement en `payment_status: 'paid'` et `diagnostic_paid: true`. Un script d'auto-correction au démarrage du serveur (`app.js`) a également mis à jour en masse toutes les souscriptions et interventions à 0 FCFA qui étaient restées bloquées en `pending`.

[2026-07-28] | Affichage du badge 'Gratuit' pour les offres, souscriptions et diagnostics à 0 FCFA | Sur le Dashboard Admin (`MaintenanceOffersPage.tsx`, `InterventionsPage.tsx`) et l'application mobile (`subscriptions_screen.dart`, `maintenance_offers_screen.dart`), le badge de paiement affiche désormais la mention élégante "Gratuit" (bleu) au lieu de "Payé" lorsque `price === 0`, `diagnostic_fee === 0` ou `is_free_diagnosis === true`.

[2026-07-28] | Déploiement complet du Dashboard Web React sur le VPS | Pour déployer les modifications du Dashboard Admin (`mct-maintenance-dashboard`), exécuter `npm run build` localement, puis synchroniser le dossier `build/` vers `/var/www/smartmaintenance/mct-maintenance-dashboard/build/` sur le VPS `77.42.22.25`. Nginx sert directement le bundle statique optimisé.

[2026-07-28] | Fix de la redirection 404 au clic sur les notifications d'intervention | La redirection des notifications poussées (`action_url: "/interventions/206"`) renvoyait une erreur 404 car la route `/interventions/:id` n'existait pas dans `App.tsx`. Ajout de la route `/interventions/:id` dans `App.tsx` et intégration de `useParams<{ id?: string }>()` dans `InterventionsPage.tsx` pour charger l'intervention et ouvrir automatiquement le modal de détails sans rechargement ni erreur 404.

[2026-07-28] | Fix de l'erreur 500 sur /suggest-technicians et fallback des techniciens | Lorsque aucun technicien n'était marqué `availability_status = 'available'`, l'algorithme `SchedulingService.suggestTechnicians` levait une exception `Aucun technicien disponible`, provoquant une réponse HTTP 500. Ajout d'un fallback automatique vers tous les techniciens actifs si aucun n'est marqué disponible, et gestion propre dans `interventionController.js` pour renvoyer une réponse HTTP 200 avec une liste vide au lieu de planter l'API.

[2026-07-28] | Flux complet de paiement échelonné 50/50 (Acompte & Solde à la fin d'intervention) | 1) Ajout des colonnes `payment_option`, `total_price`, `second_payment_amount`, `second_payment_status` et `second_payment_date` sur le modèle `Intervention.js` avec auto-migration au démarrage dans `app.js`. 2) Lors de la création d'une demande avec option acompte 50% (`payment_option: 'split'`), l'intervention calcule `diagnostic_fee` (50% acompte), enregistre `second_payment_amount` (50% solde) et initialise `second_payment_status: 'pending'`. 3) Dans `interventionController.js` (`confirmInterventionCompletion`), dès la fin de l'intervention et la confirmation du rapport, l'API vérifie le solde 50% en attente (sur l'intervention, le devis ou la souscription) et injecte `payment_required: true` avec la notification push urgente pour le paiement du solde 50%. 4) Dans `fineoPayController.js`, la réception du paiement de l'étape 2 passe `second_payment_status` à `'paid'`, complétant ainsi le cycle de paiement split 50/50.

[2026-07-28] | Fix de l'erreur "Commande non trouvée" lors du paiement du solde de l'intervention | Dans `intervention_detail_screen.dart`, la redirection lors du clic sur "Payer maintenant" (solde 50%) exigeait obligatoirement un `order_id` non nul (réservé aux devis/commandes boutique). Pour les interventions directes ou demandes d'entretien en 50/50 sans commande rattachée, `order_id` était `null`, déclenchant l'affichage du message d'erreur "Erreur: Commande non trouvée". Correction apportée pour rediriger proprement vers `DiagnosticPaymentScreen` avec le montant du solde lorsque `type == 'intervention'` ou `order_id == null`, et ajout de la carte dédiée `_buildSecondPaymentSection` pour le paiement direct du solde de 50%.

[2026-07-28] | Fix de la visibilité résiduelle du bouton "Payer le solde maintenant" après paiement | 1) Dans `fineoPayController.js` (`handleDiagnosticPayment` et `verifyDiagnosticPaymentStatus`), le blocage anticipé `if (intervention.diagnostic_paid === true) return;` empêchait la recherche de transaction et le passage de `second_payment_status` à `'paid'` lors de la vérification du solde. Correction pour n'interrompre l'API que si `second_payment_status !== 'pending'`. 2) Utilisation du montant du solde `second_payment_amount` (au lieu du seul `diagnostic_fee`) lors du matching de transaction pour le 2ème versement. 3) Inclusion explicite du champ `second_payment_status` dans le JSON de réponse de `verifyDiagnosticPaymentStatus`. 4) Dans `diagnostic_payment_screen.dart`, la boucle de polling valide le paiement dès que `second_payment_status == 'paid'`, fermant immédiatement le modal avec confirmation.

[2026-07-29] | Libellés dynamiques de paiement & Affichage du Solde (50%) sur le Dashboard Admin | 1) Dans `interventionsService.ts`, ajout des propriétés `payment_option`, `total_price`, `second_payment_amount`, `second_payment_status` et `second_payment_date` à l'interface `Intervention`. 2) Dans `InterventionsPage.tsx`, remplacement du libellé statique "Paiement diagnostic:" par des libellés dynamiques contextualisés ("Premier acompte maintenance (50%)", "Paiement maintenance", "Paiement dépannage", "Paiement installation"). 3) Ajout du bloc dédié "💳 Paiement du solde (50% restant)" affichant le statut (✓ Payé / ⏳ En attente fin d'intervention), le montant et la date d'encaissement. 4) Mise à jour du badge de la colonne "Paiement" dans le tableau principal ("✓ Payé 100%", "⏳ Acompte 50% Payé", "⏳ Acompte 50% Attente"). 5) Rebuild React local et déploiement du bundle optimisé sur le VPS.

[2026-07-29] | Décalage de dossier de déploiement Dashboard (VPS) : Le script deploy.sh envoyait la compilation React vers /var/www/smartmaintenance/dashboard/, alors que Nginx était configuré pour servir dashboard.sandbox.mct.ci depuis /var/www/smartmaintenance/mct-maintenance-dashboard/build. Résultat : le Dashboard servait une version figée/obsolète. | Pour tout déploiement web, toujours vérifier la directive root dans la configuration Nginx (/etc/nginx/sites-available/smartmaintenance) avant d'écrire la cible rsync du script de déploiement. Dans deploy.sh, synchroniser systématiquement vers le répertoire root officiel lu par Nginx (/var/www/smartmaintenance/mct-maintenance-dashboard/build).

[2026-07-31] | Nomenclature des références de devis (DEV-JJMMAA vs DEV-AAMMJJ) entre l'application mobile et le dashboard | L'application mobile reformattait à l'affichage les références de devis sous la forme `DEV-JJMMAA-HHMM-ID`, alors que le backend (`quoteWorkflowController.js`) et le dashboard React affichaient la version brute `DEV-AAMMJJ-HHMM-ID`. Attention : dans le Dashboard React, la route `/quotes` utilise le composant `src/pages/QuotesPage.tsx` (et non `src/pages/quotes/index.tsx`). Pour assurer une cohérence totale sur tous les supports (mobile, dashboard, PDF, emails) : 1) Générer directement la date au format `JJMMAA` dans `quoteWorkflowController.js` côté API backend. 2) Ajouter le helper `formatQuoteReference` dans `mct-maintenance-dashboard/src/utils/formatters.ts`. 3) Appliquer `formatQuoteReference(text)` dans `QuotesPage.tsx` (tableau principal des devis) et dans `QuoteDetail.tsx` (détails du devis) pour que l'affichage sur le Dashboard convertisse automatiquement les références au format unifié `DEV-JJMMAA...`.

[2026-07-31] | Affichage "Date inconnue" au lieu de "À l'instant" lors de la réception de notification Socket.IO en temps réel | Dans `notificationService.js` (backend), l'événement `new_notification` émettait `created_at: notification.created_at`. Avec les modèles Sequelize en `underscored: true`, la propriété de l'instance JS est `createdAt` (camelCase), rendant `notification.created_at` indéterminé (`undefined`) dans l'objet Socket.IO transmis en temps réel. Côté Dashboard (`NotificationBell.tsx`), la méthode `formatDate(date)` renvoyait "Date inconnue" si le champ était non renseigné. Pour résoudre : 1) Transmettre à la fois `created_at` et `createdAt` avec fallback `new Date().toISOString()` dans `notificationService.js`. 2) Faire un fallback `formatDate(notification.created_at || notification.createdAt)` dans `NotificationBell.tsx`. 3) Remplacer "Date inconnue" par par défaut "À l'instant" dans `formatDate` pour toute notification sans horodatage explicite.




























[2026-08-01] | `GET /api/interventions/:id` chargeait une intervention par clé primaire dès qu'un utilisateur était authentifié, avant tout contrôle de propriété, ce qui permettait potentiellement de lire les contacts, images et rapports d'un tiers. | Pour toute ressource métier sensible, intégrer le périmètre d'accès directement dans le `where` de la requête avant les `include` : rôles internes explicitement autorisés, client limité à ses identifiants historiques, technicien limité à son assignation, et même réponse 404 pour une ressource absente ou hors périmètre. Ajouter systématiquement des tests négatifs multi-utilisateurs.
[2026-08-01] | `createPaymentLink` envoyait à FineoPay le montant, le titre et l'étape fournis par le client avant même de charger la commande, sans vérifier son propriétaire ni son état ; un montant falsifié ou l'identifiant d'une commande tierce pouvait donc initier un checkout. | Pour toute initialisation de paiement, charger et autoriser la ressource avant l'appel fournisseur, dériver serveur le montant et l'étape depuis les données persistées, traiter séparément paiement intégral/acompte/solde, refuser les états non payables et ne jamais journaliser clé partielle ou URL de checkout. Le payload client ne doit être qu'une demande, jamais la source de vérité financière.
[2026-08-01] | Le callback FineoPay acceptait un statut `success` fourni par le réseau lorsque l'appel de vérification échouait ; sa signature facultative était calculée sur un JSON reconstruit et pouvait faire échouer `timingSafeEqual` sur une longueur inattendue. | Un webhook de paiement n'est jamais une preuve financière : relire obligatoirement la transaction via l'API authentifiée du fournisseur, comparer référence, référence métier, statut et montant, puis refuser de traiter si la confirmation manque. Si un HMAC existe, le calculer sur les octets bruts et valider format et longueur avant la comparaison en temps constant.
[2026-08-01] | Le webhook accusait réception avant la fin du traitement et ne possédait aucune clé d'idempotence en base ; deux callbacks concurrents pouvaient donc franchir les contrôles d'état en même temps, tandis qu'une erreur après le HTTP 200 ne pouvait plus être rejouée par le fournisseur. | Réserver chaque référence fournisseur dans une table dédiée munie d'une contrainte unique, distinguer `processing`/`completed`/`failed`, utiliser un bail pour reprendre un worker abandonné, refuser une même référence avec un payload différent et n'envoyer le HTTP 200 qu'après finalisation durable. Ce registre bloque les rejeux, mais les mutations financières doivent encore être regroupées dans une transaction avec verrou de ligne.
[2026-08-01] | Le code créait des paiements `provider: fineopay`, `status: completed` et `interventionId`, alors que le schéma de production excluait FineoPay, n'autorisait pas toujours `completed` et ne possédait aucune colonne `intervention_id`; `payment_id` n'était en outre pas unique. | Garder le modèle et les migrations financières strictement alignés, préférer des chaînes validées aux ENUM SQL difficiles à faire évoluer, rattacher chaque paiement à sa ressource et imposer l'unicité fournisseur/référence. Avant d'ajouter une contrainte unique à une table financière existante, détecter les doublons et interrompre la migration sans supprimer ni fusionner automatiquement les écritures.
[2026-08-01] | Une modification avait placé `throw error` dans le chemin nominal du premier paiement d'abonnement et le `catch` correspondant avalait ensuite l'erreur : le paiement était créé, la souscription non activée et le webhook pouvait être marqué terminé. | Après toute modification des chemins d'erreur, relire le bloc `try/catch` complet : aucun `throw` ne doit référencer une variable hors portée, et un handler financier ne doit jamais avaler une erreur de mutation. Ajouter un test du chemin nominal qui vérifie la mutation métier puis la finalisation du webhook.
[2026-08-01] | Les handlers boutique, diagnostic et souscription lisaient l'état, le modifiaient, créaient le paiement et journalisaient via des requêtes indépendantes ; une concurrence ou une erreur intermédiaire pouvait produire un paiement sans statut métier cohérent, ou l'inverse. | Pour confirmer un paiement, acquérir un verrou de ligne, recalculer le montant attendu depuis la ressource verrouillée, détecter la référence existante, puis exécuter ledger, mutation métier et journal dans une seule transaction gérée. Propager toute erreur pour provoquer le rollback et ne lancer notifications ou autres effets externes qu'après le commit.
[2026-08-01] | Le callback devis inférait le solde dès que le premier acompte était payé ; un rejeu retardé du premier callback pouvait donc devenir l'étape 2. Le même bloc repassait aussi l'intervention à `execution_confirmed` lors du solde et pouvait relancer les automatisations de suivi. | Pour un paiement multi-étapes, l'étape persistée lors de l'initiation est l'intention de paiement et ne doit jamais être reconstruite depuis les statuts après coup. Verrouiller Order puis Quote puis Intervention, calculer le montant de l'étape depuis le devis, et ne déclencher la transition d'exécution qu'au paiement intégral ou au premier acompte, jamais au solde.
[2026-08-01] | Les notifications et automatisations étaient exécutées après le commit financier ; si l'une échouait, le webhook devenait réessayable mais son rejeu détectait le paiement comme doublon et quittait avant de relancer l'effet, qui pouvait donc être perdu définitivement. | Écrire chaque effet post-paiement dans une outbox au sein de la même transaction que le ledger et la mutation métier, puis le traiter au moins une fois avec bail, backoff et dead-letter. Chaque notification doit avoir une clé unique par événement et destinataire afin qu'un retry ne recrée ni ligne, ni push, ni événement temps réel.
[2026-08-01] | Un test de migration réutilisait au test suivant la référence d'une base SQLite déjà fermée, ce qui provoquait un second `close()` et masquait le résultat réellement testé. | Dans les suites qui créent conditionnellement une ressource par test, remettre sa référence à `null` dans `afterEach` immédiatement après la fermeture afin que chaque cas reste isolé et que le nettoyage soit idempotent.
[2026-08-01] | Le service d'assignation automatique mettait d'abord à jour l'intervention puis envoyait directement deux notifications ; une panne après l'assignation pouvait faire croire à l'appelant que toute l'opération avait échoué, et un rejeu sautait ensuite les notifications puisque le technicien était déjà renseigné. | Permettre à l'orchestrateur durable de désactiver les effets externes du service métier, puis envoyer ces effets depuis l'outbox avec des clés stables. Au rejeu, relire l'état assigné et reprendre les notifications manquantes au lieu de relancer la mutation.
[2026-08-01] | Le callback diagnostic pouvait confirmer financièrement le paiement puis, si l'assignation ou une notification échouait, créer un journal et envoyer un message « paiement échoué » alors que FineoPay avait bien encaissé. | Séparer strictement l'état fournisseur de l'état des effets internes : après confirmation vérifiée, toute panne d'assignation ou de notification est un échec d'automatisation réessayable dans l'outbox, jamais un échec de paiement communiqué au client.
[2026-08-01] | `notifications.type` était un ENUM fermé, tandis que plusieurs parcours utilisaient déjà des types absents du schéma ; le code pouvait donc fonctionner en test puis voir ses notifications refusées selon la base déployée. | Pour les catégories métier appelées à évoluer, utiliser une chaîne de longueur bornée avec validation applicative et index, puis migrer explicitement l'ancien ENUM. Tester séparément la conversion PostgreSQL et l'acceptation des nouvelles valeurs sous SQLite avant tout déploiement.

[2026-08-13] | Sauvegardes PostgreSQL & Crontab VPS | Lors de la configuration d'un cron de sauvegarde PostgreSQL automatique sur le VPS, toujours extraire dynamiquement `DB_PASSWORD` depuis les variables d'environnement du serveur (`/var/www/.../.env`), exécuter une sauvegarde de test à blanc avec vérification du fichier `.sql.gz` généré, et vérifier `crontab -l` après écriture.
